# Tracer Bullet Brief — `TB-2026-07-18` Audit-log single-INSERT HMAC seal (peripheral INSERT-only)

## Metadata

- **ID:** `TB-2026-07-18-audit-log-insert-only-hmac-seal`
- **Status:** `done`
- **Related idea:** `I-2026-07-18-audit-log-peripheral-insert-only-hmac-sequence`
- **Parent context:** `TB-2026-07-16` follow-up; DB trust-boundary hardening; integrity defense-in-depth
- **Lane:** `D`
- **Posture:** `single-pass`
- **Created at:** `2026-07-18`
- **Updated at:** `2026-07-18`
- **Captured by:** grill-me → tracer-bullet-promote (Marc)
- **Closed at:** `2026-07-18`

## Objective

Replace the two-step HMAC seal in `AuditLogService.log()` (INSERT to obtain `audit_log_id`, then
UPDATE `entry_hmac`) with a **single INSERT** that already carries a pre-allocated `audit_log_id`,
an application-owned `created_at`, and the computed `entry_hmac` — all inside the existing
`REQUIRES_NEW` transaction. Then tighten `ezkey_auth` and `ezkey_integration` to **SELECT + INSERT**
on `ezkey_audit_log` (drop UPDATE), restoring the least-privilege intent of the role split without
losing atomicity or changing the HMAC canonical form.

## Boundaries in scope

- `ezkey-core` migration: named sequence `ezkey_audit_log_id_seq` + `DEFAULT nextval(...)` on
  `audit_log_id` (greenfield V4 baseline; accepts application-supplied values).
- `AuditLog` entity: application-assigned id (`Persistable`) — not `@GeneratedValue(SEQUENCE)`
  (Hibernate rejects `persist()` of a pre-set id under `GenerationType.SEQUENCE`).
- `AuditLogService.log()`: `nextval` → micros-truncated `created_at` → compute HMAC → single
  `save()` (INSERT); no `refresh`, no second write.
- `scripts/db/apply-grants.sql`: peripheral roles → `SELECT, INSERT` on `ezkey_audit_log`
  (drop UPDATE); `ezkey_admin` unchanged (keeps INSERT/UPDATE/DELETE).
- `scripts/db/verify-grants.sh`: negative assertion that peripheral **UPDATE** on `ezkey_audit_log`
  is denied; positive INSERT (rolled back; valid `EventType`).
- `docs/DATABASE_ROLE_PERMISSIONS_MATRIX.md`: update the `ezkey_audit_log` row + Explicit
  exceptions #1 to reflect peripheral INSERT-only.
- Unit test `AuditLogServiceSingleInsertSealTest`: single `save`, id/`created_at` before HMAC.

## Out of scope

- Changing the HMAC algorithm, key handling, or `buildCanonicalForm` field order/semantics.
- DB-side HMAC (trigger/PL) or admin-batch sealing.
- Applying the same identity redesign to `ezkey_auth_attempt` or other IDENTITY tables (pattern is
  reusable but not part of this slice).
- Row-level security / column-level grants.
- `ezkey_keyset_blob` Admin-first bootstrap and alert retention/purge (separate ideas).
- **Audit write fail-open / exception swallow in `AuditLogService.log()`** — delivery guarantee and
  honesty messaging live in
  [`ideas/I-2026-07-18-audit-log-fail-open-exception-swallow.md`](ideas/I-2026-07-18-audit-log-fail-open-exception-swallow.md)
  (`captured`, `P2`). This TB does not change fail-open behaviour.

## Implementation notes (delivery deltas vs grill)

| Grill intent | Delivered form | Why |
|--------------|----------------|-----|
| `GENERATED BY DEFAULT AS IDENTITY` + named sequence | Explicit `CREATE SEQUENCE` + `DEFAULT nextval(...)` + `OWNED BY` | Identity-owned sequences are hidden from Hibernate schema validation (`missing sequence`). |
| JPA `GenerationType.SEQUENCE` | Application `nextval` + assigned id + `Persistable` INSERT | Hibernate throws `Detached entity passed to persist` when id is pre-set under `@GeneratedValue(SEQUENCE)`. Setting HMAC after `save()` caused a forbidden UPDATE. |
| App-owned micros `created_at` | Delivered as designed | No `refresh` / `RETURNING` needed. |

Atomicity, canonical form, and uniform sealing path across all three APIs are unchanged in intent.

## Exit criteria — met

- Audit events from Admin / Auth / Integration APIs write with a single INSERT and verify
  (`AuditIntegrityElectiveTest`: 1146 signed entries, `invalidEntries=0`).
- `ezkey_auth` / `ezkey_integration`: SELECT + INSERT only on `ezkey_audit_log` (UPDATE denied;
  DELETE denied) — `verify-grants.sh --docker` green.
- `./scripts/build.sh` baseline green (incl. `AuditLogServiceSingleInsertSealTest`).
- Functional suite green under segregated roles.
- Matrix + Explicit exceptions #1 updated; `I-2026-07-18` → `done`; `TB-2026-07-16` follow-up
  marked resolved.

## Evidence (2026-07-18)

| Gate | Result |
|------|--------|
| `./scripts/build.sh` | PASS (unit: `AuditLogServiceSingleInsertSealTest`) |
| Clean-start (segregated roles) | PASS |
| `mvn test -pl ezkey-tests` | PASS (146 tests, 0 failures) |
| `mvn test -pl ezkey-tests -P elective-tests -Dtest=AuditIntegrityElectiveTest` | PASS (`invalidEntries=0`, all three APIs signed) |
| `./scripts/db/verify-grants.sh --docker` | PASS (peripheral UPDATE denied; INSERT allowed) |

## Links

- Idea: [`ideas/I-2026-07-18-audit-log-peripheral-insert-only-hmac-sequence.md`](ideas/I-2026-07-18-audit-log-peripheral-insert-only-hmac-sequence.md)
- Grill: [`grill-sessions/2026-07-18-audit-log-insert-only-hmac-sequence-grill-me.md`](grill-sessions/2026-07-18-audit-log-insert-only-hmac-sequence-grill-me.md)
- Parent TB: [`TB-2026-07-16-postgresql-application-role-split.md`](TB-2026-07-16-postgresql-application-role-split.md)
- Matrix: [`../../../docs/DATABASE_ROLE_PERMISSIONS_MATRIX.md`](../../../docs/DATABASE_ROLE_PERMISSIONS_MATRIX.md)
- Code: `org.ezkey.audit.service.AuditLogService`, `org.ezkey.audit.integrity.AuditHmacService`, entity `org.ezkey.audit.domain.entity.AuditLog`
- Migration: `ezkey-core/src/main/resources/db/migration/V4__partitioning_auth_audit_and_function.sql`
- Grants: `scripts/db/apply-grants.sql`, `scripts/db/verify-grants.sh`
