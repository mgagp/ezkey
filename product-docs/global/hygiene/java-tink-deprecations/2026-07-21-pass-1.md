# Assessment curated campaign - 2026-07-21 pass-1

## Metadata

- **Date:** 2026-07-21
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Java key-management subsystem using Google Tink; identify deprecated APIs currently used and safest migration targets; exclude behavior changes and broad refactors, prioritize quick wins with stability.
- **Assessment register:** `docs/security/java-tink-deprecated-api-assessment-2026-07.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-tink-deprecations/`
- **Method:** white-box / static
- **Operator:** Marc (+ agent)
- **Status:** Open

## Lot under review

1. `TINK-DEP-001` - Deprecated primitive acquisition path (`KeysetHandle.getPrimitive(Class<P>)`) - **P1**.
2. `TINK-DEP-002` - Deprecated keyset metadata access path (`KeysetHandle.getKeysetInfo()`) - **P1**.
3. `TINK-DEP-003` - Deprecated API leak in manager public contract (`getKeysetInfo()`) - **P2**.

Agent briefing style: one finding at a time; decisions recorded after HITL.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `TINK-DEP-001` | P1 | fix | Implemented on `hygiene/tink-deprecated-qw1-primitive-acquisition`; top-level reactor `mvn checkstyle:check` passed |
| 2 | `TINK-DEP-002` | P1 | fix | Implemented in `TinkKeyManager` via Entry-based key ID access; targeted tests and top-level reactor Checkstyle passed |
| 3 | `TINK-DEP-003` | P2 | pending | HITL required |

Decision values:

- **fix** - change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** - acknowledged; later pass
- **suppress** - intentional posture; reason recorded
- **skip** - duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - TINK-DEP-001

Confirmed by compiler deprecation warning in Tink 1.23.0. Replacement is straightforward and behavior-preserving (explicit configuration path).

### 2 - TINK-DEP-002

Wider footprint than item 1 (rotation/add/promote/info reads), still an internal call-shape migration. Needs careful characterization because key ID extraction is central to rotation logic.

### 3 - TINK-DEP-003

Public contract currently leaks deprecated shape. Good long-term hygiene, but can be deferred if API stability risk is judged higher than immediate value.

## Follow-ups

- No handoff opened yet.
- No `I-*` / `TB-*` promotion (not needed for this hygiene pass).
- Assessment dispositions pending HITL decisions.

## Out of scope this pass

- Any crypto behavior change (algorithms, key format, storage strategy).
- Large refactor of key-management architecture.
- Non-Tink deprecation cleanup.
