# Mobile doctor-curated campaign — 2026-09-17 pass-1

> **Status: HITL decisions closed; fixes done on branch.** Lot decisions (Marc 2026-09-17):
> **#1–#4** = **fix** (implemented and pushed on this PR); **#5** = **defer** → handoff
> **Christophe**; **#6** = **defer** (intentional Play fail-open; no narrow-catch rewrite).
> Operator: Marc. Execution agent: Fred / Cursor Cloud.

## Metadata

- **Date:** 2026-09-17
- **Branch / PR:** `cursor/mobile-doctor-2026-09-17-pass-1-810a` — draft PR [#561](https://github.com/mgagp/ezkey/pull/561)
- **Base SHA:** `a153602a82eaa01c6ed53fea728bd8c7329d78d3` (same as `mobile2` / FF from `origin/main`)
- **Command:** `yarn doctor:curated` (from `ezkey_mobile/`; no `--skip-*`)
- **Report:** `logs/mobile-doctor/mobile-doctor.curated.md` (local, gitignored)
- **Operator:** Marc (+ Fred / agent)
- **Companion Gradle probe:** `bash ./gradlew help --warning-mode all` with `JAVA_HOME=JDK 21`
  (failed later on missing Android SDK — expected on this cloud VM; deprecation lines still emitted)
- **Justin F dump:** Confirmed the same app-owned space-assignment warnings under
  `ezkey_mobile/android/app/build.gradle` (not `node_modules` / RN library Gradle).

## Tool run summary

| Tool | Result | Notes |
|------|--------|-------|
| react-doctor | PASS | 17 raw diagnostics |
| Semgrep (Docker `semgrep/semgrep`) | PASS | 0 findings (Ezkey mobile pack) |
| Detekt CLI 1.23.8 | PASS | Ran under default VM JDK 25; emitted `sun.misc.Unsafe` toolchain deprecation (tooling noise) |
| Curator | PASS | Raw 38 → curated 34; suppressed 4 (pass-1 2026-08-25 rules) |
| Full `./gradlew` assemble / device install | NOT RUN on cloud | No `ANDROID_HOME` / SDK — Fred coordinates Justin post-#1 smoke on F |

## Lot under review

Brief numbered overview only (rule + location hint). Keep small (≈3–6).

1. **Gradle Groovy space-assignment deprecations** — `ezkey_mobile/android/app/build.gradle` (ndkVersion,
   buildConfig, namespace, signingConfig, shrinkResources) — Gradle 10 removal — **HITL fix**
2. `js-set-map-lookups` — `ezkey_mobile/app/screens/Home/HomeScreen.tsx` — **HITL fix**
3. `rerender-lazy-state-init` — `ezkey_mobile/app/screens/Language/LanguageScreen.tsx` — **HITL fix**
4. DangerZone FlatList pair — `rn-no-inline-flatlist-renderitem` + `rn-list-callback-per-row`
   (`ezkey_mobile/app/screens/DangerZone/DangerZoneScreen.tsx`) — **HITL fix (local)**
5. Crypto dead symbols — Detekt `UnusedParameter` (`securityLevel`) +
   `UnusedPrivateProperty` (`SECURITY_LEVEL_CONFIRM_BEFORE_APPROVALS`) in `EzkeyCryptoModule.kt`
   — **HITL defer → Christophe**
6. Low-signal allotment: `TooGenericExceptionCaught` ×3 — `EzkeyPlayUpdateModule.kt` (fail-open Play)
   — **HITL defer**

**Agent briefing style:** do not use a dense options matrix as the primary HITL vehicle. Iterate
one finding at a time in dialogue with the operator; record the final decisions table here after
HITL closes.

## Decisions

| # | Finding | Decision | Action |
|---|---------|----------|--------|
| 1 | Gradle space-assignment — `ezkey_mobile/android/app/build.gradle` | **fix** (Marc GO 2026-09-17) | Rewrite listed props to `prop = value` only. Justin F dump confirmed same app-owned lines. |
| 2 | `js-set-map-lookups` — `HomeScreen.tsx` | **fix** (Marc GO 2026-09-17) | Derive `expandedInstallationIds` Set; use `.has` in the installation-group map. |
| 3 | `rerender-lazy-state-init` — `LanguageScreen.tsx` | **fix** (Marc GO 2026-09-17; overrides prior skip note) | Lazy `useState(() => normalizeLocale(...))`. |
| 4 | DangerZone FlatList inline `renderItem` / per-row `onPress` | **fix** (Marc GO 2026-09-17, local) | Memoized row + `renderEnrollmentItem` useCallback; stable `handlePress`. |
| 5 | Crypto unused `securityLevel` / constant | **defer** → handoff **Christophe** (Marc 2026-09-17) | No `EzkeyCryptoModule` / crypto bridge change this pass. |
| 6 | Play `TooGenericExceptionCaught` | **defer** (Marc 2026-09-17) | Intentional Play fail-open; no narrow-catch rewrite this pass. |

Decision vocabulary used in HITL closeout (after operator replies):

- **fix** — source change accepted after HITL
- **suppress** — intentional / accepted noise; reason in suppressions file
- **skip** — fuzzy or not worth inventing work

Also remember: **if it ain't broken, don't fix it** — especially in crypto / keystore / proof-token zones.

## Rationale (short)

### 1 — Gradle space-assignment (listing warnings) — **fixed**

Cloud probe with `--warning-mode all` reproduced Groovy DSL space-assignment deprecations
scheduled for removal in Gradle 10. Justin’s F dump confirmed the same warnings are
**app-owned** under `ezkey_mobile/android/app/build.gradle` (ignore RN/`node_modules` Gradle).
Marc GO 2026-09-17: rewrite assignment syntax only for `ndkVersion`, `buildConfig`, `namespace`,
`signingConfig` (debug + both release branches), and `shrinkResources`. No other hygiene items.

### 2 — `js-set-map-lookups` — **fixed**

`expandedInstallations.includes(group.installation.id)` inside the installation-group map was a
classic O(n²) micro-smell. Marc GO 2026-09-17: keep `string[]` state (toggle / effect unchanged);
derive `expandedInstallationIds = useMemo(() => new Set(expandedInstallations), …)` and use
`.has` in the map. Same pattern as existing `brokenIds`.

### 3 — `rerender-lazy-state-init` — **fixed**

`useState(normalizeLocale(...))` re-evaluated the initializer every render (result discarded after
first mount). A brief **skip** was recorded when the HITL widget went unanswered; Marc then
**GO fix now** (2026-09-17), overriding that skip. Implemented:
`useState(() => normalizeLocale(...))`. No i18n/UX behavior change.

### 4 — DangerZone FlatList pair — **fixed**

Doctor flagged inline `renderItem` and rebuilt row `onPress`. Marc GO 2026-09-17 (local only):
extract same-file `DangerZoneEnrollmentRow` (`React.memo` + `useCallback` `handlePress`) and
`renderEnrollmentItem` useCallback. UI unchanged; no shared list-pattern / Patrick-scale redesign.

### 5 — Crypto unused symbols — **deferred (Christophe)**

`generateEnrollmentKeyPair(..., securityLevel: String?)` currently ignores `securityLevel`
(Javadoc says unknown/null falls back). Constant
`SECURITY_LEVEL_CONFIRM_BEFORE_APPROVALS` is unused. Marc 2026-09-17: **defer** → handoff
**Christophe** (trust / crypto ownership). No source change to `EzkeyCryptoModule` or the crypto
bridge in this pass — if it ain’t broken, don’t fix it.

### 6 — Play fail-open catches — **deferred**

`EzkeyPlayUpdateModule` intentionally catches broad `Exception` and resolves “no update” so
enrollment / auth stay unblocked. Marc 2026-09-17: **defer** — keep fail-open posture; no
narrow-catch rewrite this pass (would need a Play SDK exception map and device evidence).

## Deprecated / toolchain inventory (outside or adjacent to lot)

| Item | Signal | Proposed disposition | Owner hint |
|------|--------|----------------------|------------|
| AGP opt-outs in `android/gradle.properties` (`android.newDsl=false`, `android.builtInKotlin=false`, R8/resvalues flags, …) | Silent opt-outs added 2026-03; docs say AGP 9 rides with RN **0.87** program | **defer** | **Alex** (program-sized) |
| Yarn peer: `@babel/core` 8.0.1 vs Babel-7 peer ranges from RN preset plugins | `YN0060` on `yarn install` | **defer** (known Babel 8 posture; AGENTS.md worklets note) | **Alex** with 0.87 train |
| Yarn peer: `eslint` 9.39.4 vs `eslint-plugin-ft-flow` wanting `^8.57` | `YN0060` | **defer** | tooling / later ESLint pack |
| Detekt on JDK 25 → `sun.misc.Unsafe::objectFieldOffset` deprecated | Tooling warning during doctor | **defer** | Prefer JDK 21 for Detekt (**Justin** / agent env) |
| `rn-prefer-pressable` mass (`TouchableOpacity` still widespread) | Already suppressed | **defer** | **Patrick** craft campaign if ever funded |
| `rn-no-legacy-shadow-styles` on Home FAB | Android-first; `elevation` intentional | **defer** / skip | iOS milestone later |
| `no-array-index-as-key` on `PinCodeInput` | Fixed-length digit boxes; index is stable | **skip** (false-positive posture) | — |
| `no-derived-state` on `EnrollmentScannerModal` | `isActive` mirrors `visible` but also forced false after scan | **skip** / leave | camera path — do not “simplify” blindly |
| ComingSoon / Settings ScrollView doctor nags | Low product value screens / padding pattern | **defer** | — |
| Detekt crypto maintainability (`ReturnCount`, `UseCheckOrError`, `TooManyFunctions`, `ThrowsCount`) | Same out-of-scope as 2026-08-25 pass-1 | **defer** | crypto — Christophe only if funded |
| No Dependabot `npm` directory for `/ezkey_mobile` | Config gap vs Admin UI / SDK | **handoff** | **Alex** (program / hygiene config) — not this lot |
| Open Dependabot PRs touching mobile | None open (only deferred TS elsewhere) | n/a | — |

## Justin F (post-#1 — Fred coordinates)

Justin already confirmed the app-owned deprecation lines. After this #1 land:

1. Smoke `yarn android:install:debug:clean` (or release path) on device F.
2. Optional: re-run `--warning-mode all` and confirm the listed space-assignment warnings are gone
   from `ezkey_mobile/android/app/build.gradle`.

Do **not** invent craft/security ownership; hand those to Patrick / Christophe as above.

## Follow-ups

- HITL lot decisions: **closed** (#1–#4 fix done on branch; #5 defer→Christophe; #6 defer)
- Deferred / handoff: #5 → Christophe (crypto unused symbols; no bridge edit this pass)
- Deferred: #6 Play fail-open catches (leave as-is)
- Deferred inventory: crypto Detekt maintainability; AGP 9 / RN 0.87; Pressable mass; Dependabot mobile directory
- Suppressions: none applied
- Code touched (fixes on branch):
  - #1: `ezkey_mobile/android/app/build.gradle` — commit `f0a253e3`
  - #2: `ezkey_mobile/app/screens/Home/HomeScreen.tsx` — commit `08955ec8`
  - #3: `ezkey_mobile/app/screens/Language/LanguageScreen.tsx` — commit `36fa8f87`
  - #4: `ezkey_mobile/app/screens/DangerZone/DangerZoneScreen.tsx` — commit `b2bda8e5`
  - Campaign note: `product-docs/global/hygiene/mobile-doctor/2026-09-17-pass-1.md`

## Out of scope this pass

Rewrite mass (`rn-prefer-pressable`, barrel imports), crypto Detekt maintainability (same as
pass-1), crypto bridge edits for #5, Play catch narrowing for #6, pentest, exploratory QA, blind
Dependabot merges, device install (Justin via Fred).
