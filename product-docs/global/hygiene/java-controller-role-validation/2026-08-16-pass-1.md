# Assessment curated campaign - 2026-08-16 pass-1

## Metadata

- **Date:** 2026-08-16
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Java controllers across Admin / Auth / Integration /
  Crypto / Demo Device / Demo ACME; role validation and Lifecycle Governance (Global vs Tenant,
  no cross-tenant Tenant Admin access). Reassess the shared-`ROLE_ADMIN` class identified in
  SEC-017 and in the 2026-08 dead-path pass (CTRL-DEAD-003). Static analysis only. Non-goals:
  live pentest/DAST, Admin UI gating, implementing fixes until HITL, `I-*`/`TB-*` unless later
  promoted.
- **Assessment register:** `docs/java-controller-role-validation-assessment-2026-08.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-controller-role-validation/`
- **Method:** white-box / static (no execution)
- **Operator:** Marc (+ agent)
- **Status:** Open

## Lot under review

1. `CTRL-ROLE-001` — Admin enrollment QR skips object-level tenant check (invite-secret distribution; Auth API bind remains token-gated) — **P1**
2. `CTRL-ROLE-002` — Shared `ROLE_ADMIN` gate makes tenant isolation opt-in — **P1**
3. `CTRL-ROLE-003` — Cross-tenant deny is 403 on some routes, 404 on others — **P2**
4. `CTRL-ROLE-004` — Mixed enforcement dialects on the same controller — **P2**
5. `CTRL-ROLE-005` — Living security matrix still describes `ROLE_ADMIN` as unrestricted — **P3**

Category 2 (noted, not HITL this pass): TA cannot deactivate peers; Global Admin cross-tenant
operator writes are intentional; unused unscoped `findAll` service helpers; CTRL-DEAD-003
revisit remains sound; Auth/Integration/Crypto/Demo have no admin-role bleeding surface.

Agent briefing style: one finding at a time; decisions recorded after HITL.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `CTRL-ROLE-001` | P1 | fix | Implemented 2026-08-18; closed in [PR #470](https://github.com/mgagp/ezkey/pull/470) |
| 2 | `CTRL-ROLE-002` | P1 | fix | Implemented 2026-08-20; convention in `ezkey-admin-api/AGENTS.md` § Authorization at controllers |
| 3 | `CTRL-ROLE-003` | P2 | fix | Implemented 2026-08-20; hide-existence 404 in `ezkey-admin-api/AGENTS.md`; analysis: `HANDOFF-ctrl-role-003b-cross-tenant-deny-semantics-analysis.md` |
| 4 | `CTRL-ROLE-004` | P2 | fix | Implemented 2026-08-20; dialects in `ezkey-admin-api/AGENTS.md`; analysis: `HANDOFF-ctrl-role-004b-authorization-dialect-coherence-analysis.md` |
| 5 | `CTRL-ROLE-005` | P3 | fix | Handoff: `product-docs/global/backlog/handoffs/HANDOFF-ctrl-role-005-security-matrix-role-story.md` |

Decision values:

- **fix** — change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** — acknowledged; later program or next pass
- **suppress** — intentional posture; reason recorded
- **skip** — duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - CTRL-ROLE-001

Operator chose **fix**. Auth API bind stays token-gated (no tenant filter). The defect is Admin
API QR distributing the bind handle (`id` + proof token) to a Tenant Admin who already gets
403 on JSON GET. Harm is consumption of a pending invite (`CREATED` → `BOUND`), not automatic
MFA takeover (challenge is a second envelope; wrong verify burns the row). Human walkthrough
and sequence diagrams live in the assessment register.

**Closeout 2026-08-19:** `getQrCode` calls `canAccessEnrollment` and denies with HTTP 404.
Coverage: `EnrollmentControllerQrAccessTest` plus `TenantCrossIsolationSecurityTest` QR
deny/own-tenant rows; live stack confirmed 404 for a foreign Tenant Admin and 200 for an
own-tenant CREATED invite. Ephemeral handoff deleted.
PR: [#470](https://github.com/mgagp/ezkey/pull/470). JSON GET remaining 403s stay with 003b.

### 2 - CTRL-ROLE-002

Operator chose **fix**. Durable controller convention: `ROLE_ADMIN` means any authenticated
admin, not tenant isolation and not Global-only. Function split uses `GLOBAL_ADMIN` /
`TENANT_ADMIN`; object split uses `AccessControlService` (or equivalent). Greenfield would
drop the `ROLE_ADMIN` umbrella and annotate `hasAnyRole(GLOBAL, TENANT)` vs
`hasRole(GLOBAL)` — that is honesty, not a substitute for object checks. Mass annotation
rewrite is optional in the same handoff, not required to close the class.

**Closeout 2026-08-20:** convention written in `ezkey-admin-api/AGENTS.md` § Authorization
at controllers. No Java annotation migration. Ephemeral handoff deleted. Remodeling
analysis remains `HANDOFF-ctrl-role-002b-role-model-remodeling-analysis.md`.

### 3 - CTRL-ROLE-003

Operator chose **fix** with the 002-style split. Immediate: write get-by-id
hide-existence as **404**, apply it to enrollment QR (with 001). Honest note: JSON
GET enrollment/integration is still 403 today; that is not rewritten in this slice.
Complementary **003b** analysis decides whether to unify the rest, document a
deliberate split, or stop.

**Closeout 2026-08-20:** convention written in `ezkey-admin-api/AGENTS.md` § Authorization
at controllers. QR 404 already landed with 001. No mass-edit of JSON GET 403s.
Ephemeral handoff deleted. Analysis remains
`HANDOFF-ctrl-role-003b-cross-tenant-deny-semantics-analysis.md`.

### 4 - CTRL-ROLE-004

Operator chose **fix + 004b**, continuing the design-integrity thread (roles, deny
semantics, dialects). Immediate: AGENTS convention — dialect 1 (`canAccess*`) on
touch; QR must not invent a fourth style. Retire/delete inline (dialect 2) stays
until 004b. Analysis family: 002b (who), 003b (HTTP deny), 004b (where the object
check lives).

**Closeout 2026-08-20:** dialect convention written in `ezkey-admin-api/AGENTS.md` §
Authorization at controllers. No Java rewrite of integration retire/delete.
Ephemeral handoff deleted. Analysis remains
`HANDOFF-ctrl-role-004b-authorization-dialect-coherence-analysis.md`.

### 5 - CTRL-ROLE-005

Operator chose **fix** (option 1, no 005b). Rewrite the matrix **role story** (table,
“No ownership restrictions”, Scenario 4) so it stops teaching pre-SEC-017. Endpoint
rows stay a lagging sketch; honesty banner + pointer to Admin API `AGENTS.md`.

## Follow-ups

- Ephemeral handoffs: 001 deleted ([PR #470](https://github.com/mgagp/ezkey/pull/470)); 002, 003, and 004 deleted (2026-08-20, `main`); 002b, 003b, 004b, 005 still open
- `I-*` / `TB-*` promoted: none
- Assessment dispositions: 001 implemented 2026-08-18; 002–004 implemented 2026-08-20; 005 fix accepted (HITL complete)
- 001: `canAccessEnrollment` + 404 on Admin QR; unit + isolation tests; Auth API bind unchanged
- 002: Admin API `AGENTS.md` authorization convention; no filter/annotation rewrite
- 003: get-by-id hide-existence 404 in the same `AGENTS.md` subsection; JSON GET 403s unchanged
- 004: object-check dialects 1–3 in the same subsection; retire/delete left as dialect 2
- Analysis-only 002b/003b/004b wait for later sessions

## Out of scope this pass

- Runtime / DAST / `security-pentest-curated`
- Admin UI role visibility
- Mass annotation rewrite of every `hasRole('ADMIN')`
- Drive-by product change to let Tenant Admins deactivate peers
- Crypto API remaining unauthenticated (lab tool by design)
