# Assessment curated campaign - 2026-07-26 pass-1

## Metadata

- **Date:** 2026-07-26
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Java JSON processing (`JsonNode` / Jackson and any
  other JSON libraries in Ezkey Java modules); inventory deprecated methods; fix only documented
  low-risk quick wins to keep warning volume low. Non-goals: fix-in-session until HITL,
  zero-warning campaigns, doctor/Dependabot lanes, annotation JSTEP-1 migration, `I-*`/`TB-*`
  unless later promoted.
- **Assessment register:** `docs/java-json-deprecated-api-assessment-2026-07.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-json-deprecations/`
- **Method:** white-box / static (Jackson databind 3.1.5 sources as deprecation authority)
- **Operator:** Marc (+ agent)
- **Status:** Closed

## Lot under review

1. `JSON-DEP-001` — Auth `RateLimitFilter` `isTextual` / `asText` — **P2** (category 1).
2. `JSON-DEP-002` — Test / harness `asText` / `asText(null)` / `isTextual` — **P3** (category 1).
3. `JSON-DEP-003` — Test harness `textNode` factory — **P3** (category 1).

Category 2 (redesign-heavy deprecated usage): **none found**.

Agent briefing style: one finding at a time; decisions recorded after HITL.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `JSON-DEP-001` | P2 | fix | Migrated `RateLimitFilter` to `isString()` / `asString()` in-session; no handoff |
| 2 | `JSON-DEP-002` | P3 | fix | Bulk renamed test/harness `asText` / `isTextual` → `asString` / `isString`; no handoff |
| 3 | `JSON-DEP-003` | P3 | fix | `textNode` → `stringNode` in `BootstrapCredentialsExtractor`; no handoff |

Decision values:

- **fix** - change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** - acknowledged; later pass
- **suppress** - intentional posture; reason recorded
- **skip** - duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - JSON-DEP-001

Official Jackson 3.1.5 replacements are rename aliases (`asText` → `asString`, `isTextual` →
`isString`). Operator chose **fix** in-session. Applied in both enrollmentId and authAttemptId
extraction branches of `RateLimitFilter`. Fail-open `null` catch path unchanged.

### 2 - JSON-DEP-002

Same rename aliases across functional-test helpers and `QrCodePayloadServiceTest` (~22 call sites).
Operator chose **fix** in-session. Includes `asText(null)` → `asString(null)` in churn state
loader.

### 3 - JSON-DEP-003

`JsonNodeCreator.textNode` → `stringNode` (Jackson 3.0 factory rename). Operator chose **fix**
in-session. Single call site in `BootstrapCredentialsExtractor.saveCredentialsToFile`.

## Follow-ups

- Assessment dispositions: all three findings closed.
- No handoffs opened (all fixed in-session).
- No residual `asText` / `isTextual` / `textNode` call sites in Ezkey Java sources after this pass.

## Out of scope this pass

- Implementing remediations before per-finding HITL.
- Treating `asInt()` / `asLong()` / `asBoolean()` as debt (not deprecated in Jackson 3.1.5).
- Migrating `com.fasterxml.jackson.annotation` usage (JSTEP-1).
- Non-deprecation JSON redesign (DTO shapes, RestAssured mapping strategy).
