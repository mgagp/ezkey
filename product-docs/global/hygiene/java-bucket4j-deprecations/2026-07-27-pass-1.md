# Assessment curated campaign - 2026-07-27 pass-1

## Metadata

- **Date:** 2026-07-27
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Java Bucket4j rate-limit call sites (`ezkey-auth-api`,
  `ezkey-admin-api`, `ezkey-integration-api`, `ezkey-demo-app-acme`); inventory deprecated
  `Bandwidth.classic` / `Refill` APIs on Bucket4j 8.19.0; prefer mechanical builder migration
  already proven in Admin / Integration modules. Non-goals: fix-in-session until HITL,
  rate-limit policy redesign, distributed Bucket4j, zero-warning campaigns, doctor/Dependabot
  lanes, `I-*`/`TB-*` unless later promoted.
- **Assessment register:** `docs/java-bucket4j-deprecated-api-assessment-2026-07.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-bucket4j-deprecations/`
- **Method:** white-box / static (+ Bucket4j 8.19.0 source `@Deprecated` confirmation)
- **Operator:** Marc (+ agent)
- **Status:** Closed

## Lot under review

1. `B4J-DEP-001` — Auth API `RateLimitFilter` still uses `Bandwidth.classic` + `Refill.intervally` — **P2** (category 1 quick win).

Category 2 (redesign-heavy deprecated usage): **none found** — sibling modules already on
`Bandwidth.builder()`.

Agent briefing style: one finding at a time; decisions recorded after HITL.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `B4J-DEP-001` | P2 | fix | Migrated `createBucket` to `Bandwidth.builder()` in-session; no handoff |

Decision values:

- **fix** — change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** — acknowledged; later program or next pass
- **suppress** — intentional posture; reason recorded
- **skip** — duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - B4J-DEP-001

Official Bucket4j 8.19.0 replacement is mechanical and behavior-preserving (same capacity +
intervally refill window). Operator chose **fix** in-session. Applied in
`RateLimitFilter.createBucket`; `Refill` import removed. Matches `AdminRateLimitFilter` pattern.

## Follow-ups

- No handoff opened (single-file mechanical fix).
- Assessment disposition for `B4J-DEP-001` set to closed.
- No further lot items this pass.

## Out of scope this pass

- Changing rate-limit thresholds, bucket keys, or Caffeine TTL.
- Dependabot Bucket4j version bumps unrelated to this API shape.
