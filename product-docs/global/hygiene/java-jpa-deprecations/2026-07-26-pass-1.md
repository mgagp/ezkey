# Assessment curated campaign - 2026-07-26 pass-1

## Metadata

- **Date:** 2026-07-26
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Java JPA / Spring Data persistence call sites
  (`ezkey-core` + API modules); inventory deprecated methods; split mechanical quick wins vs
  redesign-heavy items (concurrency / cache / query shape). Non-goals: fix-in-session until HITL,
  zero-warning campaigns, doctor/Dependabot lanes, `I-*`/`TB-*` unless later promoted.
- **Assessment register:** `docs/java-jpa-deprecated-api-assessment-2026-07.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-jpa-deprecations/`
- **Method:** white-box / static (+ `javac -Xlint:deprecation` confirmation for Criteria API)
- **Operator:** Marc (+ agent)
- **Status:** Closed

## Lot under review

1. `JPA-DEP-001` — Criteria `multiselect` on dashboard aggregates — **P2** (category 1 quick win).

Category 2 (redesign-heavy deprecated usage): **none found**.

Agent briefing style: one finding at a time; decisions recorded after HITL.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `JPA-DEP-001` | P2 | fix | Migrated both call sites to `cq.select(cb.tuple(...))` in-session; no handoff |

Decision values:

- **fix** - change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** - acknowledged; later pass
- **suppress** - intentional posture; reason recorded
- **skip** - duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - JPA-DEP-001

Official Jakarta Persistence 3.2 replacement is mechanical and behavior-preserving for `Tuple`
dashboard aggregates. Operator chose **fix** in-session. Applied in
`AuthAttemptService.aggregateDashboard24h` and `EnrollmentService.aggregateDashboardEnrollmentStats`.

## Follow-ups

- No category-2 handoff opened (empty set).
- Assessment disposition for `JPA-DEP-001` set to closed.

## Out of scope this pass

- Implementing remediations before per-finding HITL.
- Non-deprecation JPA redesign (locks, flush strategy, reencryption batching).
- Renaming Ezkey service methods called `getById` (not Spring Data deprecated APIs).
