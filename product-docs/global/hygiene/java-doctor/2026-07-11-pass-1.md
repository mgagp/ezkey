# Java doctor-curated campaign — 2026-07-11 pass-1

## Metadata

- **Date:** 2026-07-11
- **Branch / PR:** `hygiene/java-doctor-pass-1` _(PR pending)_
- **Command:** `./scripts/java-doctor-curated.sh --skip-compile` (earlier dry-run)
- **Report:** `logs/java-doctor/java-doctor.curated.md` (raw 440 → curated 173)
- **Operator:** Marc (+ agent)

## Lot under review

1. `RCN_REDUNDANT_NULLCHECK_OF_NONNULL_VALUE` — TenantController / GlobalExceptionHandle
2. `UC_USELESS_CONDITION` — TinkKeyManage
3. `ST_WRITE_TO_STATIC_FROM_INSTANCE_METHOD` — EncryptionEntityListene
4. `DMI_RANDOM_USED_ONLY_ONCE` — AuditHmacService.generateHmacKeyFile
5. `EmptyCatchBlock` / `DE_MIGHT_IGNORE` — AdminAuthService.logout
6. `UnusedLocalVariable` — IntegrationController.create (`requestAuditDetails`) — **HITL pending**

## Decisions

| # | Finding | Decision | Action |
|---|---------|----------|--------|
| 1 | RCN redundant nullcheck | **skip** | No code change; no suppress (fuzzy localization) |
| 2 | UC useless condition (TinkKeyManager) | **defer dig** | No change this PR; needs careful source localization + characterization tests before any refactor |
| 3 | ST write static (EncryptionEntityListener) | **suppress** | `suppressions.json` — intentional JPA listener static injection |
| 4 | DMI Random once (AuditHmacService) | **suppress** | `suppressions.json` — one-shot keyfile generation is acceptable |
| 5 | Empty catch logout | **fix** | Best-effort `logger.warn` with exception; API surface unchanged |
| 6 | Unused local `requestAuditDetails` | **pending** | Await Go / No-Go |

## Rationale (short)

### 1 — RCN redundant nullcheck

SpotBugs line numbers pointed at class / Javadoc, not a clear `if`. Fuzzy signal rule: do not open
bytecode to invent a defect. Skip.

### 2 — TinkKeyManager useless condition

Worth investigating later because crypto keyset management is high-sensitivity. Any change requires
source-level localization, counter-analysis, and pre/post characterization tests. Out of this
minimal pass-1 code slice.

### 3 — EncryptionEntityListener static write

Documented intentional pattern (JPA listeners are not Spring-managed). Tool correctly flags; posture
is accept + suppress with reason.

### 4 — AuditHmacService SecureRandom once

One-shot HMAC keyfile generation at first startup — not a hot path. Design is satisfactory; suppress
rather than churn crypto bootstrap for SpotBugs style.

### 5 — AdminAuthService.logout empty catch

Comment claimed logging but catch was empty. Logout must remain best-effort (no API failure). Option
A: warn-level log with exception, no sensitive token material in the message.

### 6 — UnusedLocalVariable

Still open for HITL Go / No-Go (remove vs reuse in success audit path).

## Follow-ups

- HITL on item 6
- Optional careful dig on item 2 (characterization tests first)
- Suppressions added: `ST_WRITE_TO_STATIC_FROM_INSTANCE_METHOD` (path-scoped), `DMI_RANDOM_USED_ONLY_ONCE` (path-scoped)
- Code touched: `AdminAuthService.java` (logout), `config/java-doctor/suppressions.json`, `AGENTS.md`, this hygiene folde

## Out of scope this pass

- `THROWS_METHOD_THROWS_RUNTIMEEXCEPTION` (18) and related throws-clause noise
- `CT_CONSTRUCTOR_THROW`
- PMD complexity / NPath on large controllers (design debt, not first hygiene PR)
