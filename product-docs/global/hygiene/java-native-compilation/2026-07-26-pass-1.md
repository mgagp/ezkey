# Assessment curated campaign - 2026-07-26 pass-1

## Metadata

- **Date:** 2026-07-26
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Inventory and honestly examine all Java GraalVM /
  native-image residual surface (code, POM profiles, classpath resources, Docker/ops flags,
  scripts, living docs). Treat native compilation as a **non-objective** for now; prefer clean
  removal of dead/stale material over keeping “someday” scaffolding. Non-goals: reopen native
  initiative; React Native; invent `I-*`/`TB-*` per finding; implement before per-finding HITL.
- **Assessment register:** `docs/java-native-compilation-assessment-2026-07.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-native-compilation/`
- **Method:** white-box / static
- **Operator:** Marc (+ agent)
- **Status:** Closed

## Lot under review

1. `NAT-001` — Auth + Integration RuntimeHints / `META-INF/native-image` on classpath — **P1**
2. `NAT-002` — Maven `-Pnative` profiles (parent + auth + integration) — **P1**
3. `NAT-003` — Admin API orphaned native remnants — **P2**
4. `NAT-004` — Docker / clean-start / keygen `--native` ops surface — **P1**
5. `NAT-005` — Native/AOT scripts under `scripts/` — **P2**
6. `NAT-006` — Living documentation corpus + scattered how-to references — **P2**

**Agent briefing style:** one finding at a time; record the decisions table after HITL closes.
Do not paste raw tool dumps.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `NAT-001` | P1 | fix | Deleted `AuthNativeConfiguration`, `IntegrationNativeConfiguration`, and all `META-INF/native-image/**` under auth + integration |
| 2 | `NAT-002` | P1 | fix | Removed `-Pnative` from parent, auth-api, integration-api, and ezkey-core POMs |
| 3 | `NAT-003` | P2 | fix | Deleted Admin `application-native.properties`, `NATIVE_BUILD.md`, and README pointer |
| 4 | `NAT-004` | P1 | fix | Removed native compose + `--native` ops flags; trimmed ops docs that advertised the mode |
| 5 | `NAT-005` | P2 | fix | Deleted native/AOT scripts under `scripts/` and removed README sections |
| 6 | `NAT-006` | P2 | fix | Deleted living `docs/NATIVE_*`, auth native remnants, archived native plans, and scattered Graal/native how-to refs |

Decision values:

- **fix** — change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** — acknowledged; later program or next pass
- **suppress** — intentional posture; reason recorded
- **skip** — duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - NAT-001

Operator Go: remove live AOT/native classpath noise. RuntimeHints were not profile-gated and
implied an active native posture despite the April 2026 non-viable runtime outcome. Docs that
still name these classes remain for NAT-006 editorial cleanup. Committed as
`chore(hygiene): remove GraalVM RuntimeHints and native-image metadata`.

### 2 - NAT-002

Operator Go after NAT-001 commit. Removed unused Maven `native` profiles from parent POM,
`ezkey-auth-api`, `ezkey-integration-api`, and the small `ezkey-core` companion profile.
Default JVM builds unchanged (`no-checkstyle` profile retained).

### 3 - NAT-003

Operator Go: remove Admin orphans. `AdminNativeConfiguration` was already gone; deleted the
stale properties profile, module how-to, and README index line. Auth-side
`application-native.properties` / `NATIVE_BUILD.md` remain for NAT-005/NAT-006.

### 4 - NAT-004

Operator Go: remove Docker/ops native mode. Deleted `docker-compose.native.yml` (+ docker-dev
override); stripped `--native` / `-Native` from start, clean-start, and keygen scripts; updated
ops README/test helper wording so the default JVM stack is the only advertised path. Build/debug
scripts remain for NAT-005; living `docs/NATIVE_*` corpus for NAT-006.

### 5 - NAT-005

Operator Go: delete spike tooling. Removed `build-native-aot.sh`, `test-native-build.sh`,
`measure-native-memory.*`, and `debug-aot-*.sh`, plus the Native Build Scripts block in
`scripts/README.md`.

### 6 - NAT-006

Operator Go with stronger clean-slate: remove **all** direct GraalVM/native-image documentation
and historized plans, not only living how-tos. Deleted the `docs/NATIVE_*.md` corpus (including
the April 2026 status note), Auth `NATIVE_BUILD.md` + `application-native.properties`, dedicated
archived plans under `.cursor/plans/archived` and `docs/plan/archives`, and trimmed scattered
license / multi-tenant / prompt references. Mobile `NATIVE_MODULES.md` and unrelated “native”
wording (React Native, repository-native methodology) kept intentionally.

## Follow-ups

- Ephemeral handoffs: none (fixes applied in-session)
- `I-*` / `TB-*` promoted: none
- Assessment dispositions: all NAT-001…NAT-006 closed

## Out of scope this pass

- React Native / mobile native crypto modules
- Reopening GraalVM as a product objective
- HA Docker stack
- Rewriting archived plans under `.cursor/plans/archived/` or `docs/plan/archives/`
