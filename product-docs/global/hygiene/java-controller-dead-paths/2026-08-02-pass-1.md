# Assessment curated campaign - 2026-08-02 pass-1

## Metadata

- **Date:** 2026-08-02
- **Keyword:** `assessment-curated`
- **Mandate (surface / axes / non-goals):** Java controllers across Admin / Auth / Integration /
  Crypto / Demo Device / Demo ACME; ensure controller code is alive — no dead methods or dead
  paths; static analysis only; prioritize quick wins. Non-goals: runtime execution, deep
  service-layer dead code, zero-warning campaigns, doctor/Dependabot shortlists as entry,
  program-sized Admin API-key M2M removal (TB-2026-05-25) in this pass, `I-*`/`TB-*` unless later
  promoted.
- **Assessment register:** `docs/java-controller-dead-path-assessment-2026-08.md`
- **Topic lane folder:** `product-docs/global/hygiene/java-controller-dead-paths/`
- **Method:** white-box / static (no execution)
- **Operator:** Marc (+ agent)
- **Status:** Closed

## Lot under review

1. `CTRL-DEAD-001` — Admin multi-device error advertises unimplemented `deviceHint` — **P2**
2. `CTRL-DEAD-002` — Unused auth-attempt helper parameters — **P3**
3. `CTRL-DEAD-003` — Integration API redundant ownership re-check — **P2**
4. `CTRL-DEAD-004` — Admin `waitForResponse` silent 500 catch — **P3**

Category 2 (noted, not HITL this pass): Admin M2M dual surface, overlapping API-key lists, cancel
rate-limit sharing, null-defensive trusted-proxy ternaries.

Agent briefing style: one finding at a time; decisions recorded after HITL.

## Decisions

| # | Finding | Severity | Decision | Action |
| --- | --- | --- | --- | --- |
| 1 | `CTRL-DEAD-001` | P2 | fix | In-session: Admin multi-device message aligned with Integration API (dropped phantom `deviceHint`) |
| 2 | `CTRL-DEAD-002` | P3 | fix | In-session: dropped unused `httpRequest` / `request` / `apiKeyId` helper params (Admin + Integration) |
| 3 | `CTRL-DEAD-003` | P2 | fix | In-session: removed Integration create re-check + dead `validateEnrollmentOwnership` helper |
| 4 | `CTRL-DEAD-004` | P3 | fix | In-session: log unexpected wait failures before empty 500 |

Decision values:

- **fix** — change accepted after HITL; ephemeral `HANDOFF-*.md` while coding; delete handoff on closeout
- **defer** — acknowledged; later program or next pass
- **suppress** — intentional posture; reason recorded
- **skip** — duplicate, fuzzy, or not worth acting on

## Rationale (short)

### 1 - CTRL-DEAD-001

Operator chose **fix**. Admin error text advertised `deviceHint`, which is not on the create
DTO and was never implemented (archived plan already noted the gap). Aligned with Integration
API: ask for `enrollmentId` only. No handoff — one-line controller change.

### 2 - CTRL-DEAD-002

Operator chose **fix**. Removed unused helper parameters: Admin `resolveEnrollmentId`
`httpRequest`, Admin `extractApiKeyId` servlet request (now zero-arg like Integration), and
`apiKeyId` on `validateEnrollmentOwnership` in Admin + Integration. Call sites updated. No
handoff.

### 3 - CTRL-DEAD-003

Operator chose **fix**. Integration create already enforces ownership inside
`resolveEnrollmentId`; removed the second `validateEnrollmentOwnership` call, the empty-403
catch branch, and the now-unused private helper (plus stale `AccessControlService` import /
Javadoc). Admin ownership check left intact. No handoff.

### 4 - CTRL-DEAD-004

Operator chose **fix**. Added `logger.error` with auth attempt id (and exception) before the
generic 500 on Admin `waitForResponse`. HTTP contract unchanged. No handoff.

## Follow-ups

- Ephemeral handoffs: none (all four fixes in-session)
- `I-*` / `TB-*` promoted: none
- Assessment dispositions: all HITL lot rows closed 2026-08-02
- Category 2 items (Admin M2M dual surface, API-key list overlap, etc.) remain out of scope

## Out of scope this pass

- Runtime / DAST
- Service-layer dead code outside controllers
- Drive-by removal of Admin API-key auth-attempt surface (TB-2026-05-25 / `#169`)
- API-key list endpoint consolidation
- Zero-warning / doctor-curated tool campaigns
