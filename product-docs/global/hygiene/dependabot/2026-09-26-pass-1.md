# Dependabot curated campaign — 2026-09-26 pass-1

## Metadata

- **Date:** 2026-09-26
- **Operator:** Marc via Mathieu GO (+ Fred curated pass / executor agent)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs in curated set:** #602, #593, #592, #591, #590, #603, #604, #607, #605; #606 (Orval closeout); deferred #498, #337
- **Highest accepted tier:** T3 (Admin UI react #590 + mobile Lot D); T4 Orval #606 cheap-exception closed same day
- **GitHub write identity:** cursor-github MCP as `mgagp` (squash merges) + Cloud Agent `GH_TOKEN` as `mgagp` (#606 squash + this note PR)
- **Apply path:** squash-merge existing Dependabot PRs individually in lot order. No Maven hygiene-branch re-apply. No deploy.

## Lots overview

1. **Lot A — T1** (#602 `@types/node` SDK JS; #593 `react-router-dom` Admin UI; #592 `react-i18next` Admin UI). Blast: types + Admin UI routing/i18n minors.
2. **Lot B — T2** (#591 `@playwright/test` Admin UI 1.62.1→1.63.0). Blast: e2e test runner only.
3. **Lot C — T3** (#590 `react` Admin UI 19.2.8→19.3.0). Blast: shipped Admin UI runtime React.
4. **Lot D — T3 mobile** (#603 `i18next`; #604 `react-i18next`; #607 `@tanstack/react-query`; #605 `axios`). Blast: mobile i18n / data / HTTP client.
5. **#606 Orval closeout — T4 cheap exception** (mobile `orval` 8.33.0→8.36.0). Blast: codegen pin only; validated generate+unit tests then squash-merged.

**Skipped (curated):** #498, #337 `deferred:later-train`; non-Dependabot #611 #610 #565 #539 #538 #347.

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| A | #602, #593, #592 | T1 | merge | Squash-merged. #592 conflicted after #593 lockfile → `@dependabot rebase` → landed as react-i18next **17.0.15**. |
| B | #591 | T2 | merge | Squash-merged after rebase onto post–Lot A main (`mergeable_state=clean`). |
| C | #590 | T3 | merge | Squash-merged after #591; Admin UI React 19.3.0. |
| D | #603, #604, #607, #605 | T3 | merge | Mobile sequential; #605 needed Dependabot rebase after sibling lockfile merges; CI `js-validate` + `android-jvm-unit-tests` green before merge. |
| — | #606 | T4 | **merge** (cheap exception) | Cloud closeout: exact pin `8.36.0` (no caret); `yarn generate:api` → **empty** generated-client diff; `yarn test` **42 suites / 305 tests passed**; CI CLEAN; squash-merged `bfb5f226`. |
| — | #498, #337 | T4 | defer (labeled) | `deferred:later-train` — not touched. |

**Merged this pass (10):** #602, #593, #592, #591, #590, #603, #604, #607, #605, #606.

## Rationale (short)

### Lot A — T1 SDK + Admin UI minors
Low blast: `@types/node` types-only; router/i18n path/patch-level Admin UI. Default squash after CLEAN.

### Lot B — Playwright 1.63.0
Dev/e2e runner bump; no app runtime. Merged when clean after Admin UI lockfile churn.

### Lot C — Admin UI React 19.3.0
Shipping runtime T3. Aligns Admin UI with prior mobile RN chantier (main already on RN 0.87.1 / React 19.3.0). Playwright recommended for closeout (not run in the first executor session).

### Lot D — Mobile i18n / react-query / axios
Runtime minors with shared `yarn.lock`; merged one-by-one with Dependabot rebase when needed. #605 axios waited for CI success after rebase.

### #606 Orval (closeout)
T4 codegen; standing cheap-exception path: Marc/GO after generate+unit tests. Cloud Agent closeout on 2026-09-26:

1. Merged PR head onto current `origin/main` (post–#605).
2. `yarn install` (Yarn 4.10.3 via Corepack); pin remains exact `"orval": "8.36.0"`.
3. `yarn generate:api` (orval v8.36.0) — `app/services/api/generated/` git diff **0 bytes** (empty / trivial → OK).
4. `yarn test` — Test Suites: **42 passed**, Tests: **305 passed** (exit 0).
5. PR `mergeable` + `mergeStateStatus=CLEAN`; CI `js-validate` + `android-jvm-unit-tests` success → squash-merged.

## Pulses (mandatory)

### Java BOM pulse
- Root `spring-boot.version` already **4.1.1** = latest 4.1.x → **none** (no Boot lot).

### Nested pin `google-java-format`
- Declared **1.36.1** — leave unless Spotless moved (no Spotless/Maven touch this pass).

### Mobile RN pulse
- Main already on **react-native 0.87.1** / **react 19.3.0** (prior chantier).
- No RN-core Dependabot PRs in this curated merge set.

### SEC-019 / Maven
- No Maven-touching merges → `./scripts/build.sh` **n/a** this pass.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | yes | Merged only when `mergeable` true and `mergeable_state` clean (or after rebase+CI green for #605: `js-validate` success, `android-jvm-unit-tests` success). Empty check-runs on some Dependabot heads treated as non-failing when GitHub mergeable clean (same pattern as prior passes). |
| Java BOM pulse | yes | 4.1.1 == latest 4.1.x → **none** |
| SEC-019 overrides reviewed after Boot bump | n/a | No Boot bump |
| Nested pin: `google-java-format` | yes | 1.36.1 leave |
| Mobile RN pulse | yes | RN 0.87.1 / React 19.3.0 already on main; no RN-core Dependabot PRs open in set |
| Mobile `yarn deps:monitor` | n/a | Not run this executor session |
| Dependabot mobile PRs peeled | yes | #603 #604 #607 #605 #606 merged |
| `./scripts/build.sh` | n/a | No Maven-touching PRs |
| Clean-start stack | deferred | Proportional closeout not run on cloud VM this session |
| Functional tests | deferred | |
| Elective functional | n/a | |
| Playwright Admin UI | deferred | **Recommended** after #590 React + #591 Playwright tool bump — run `./scripts/run-ui-tests.sh` on next closeout |
| Exact pin preserved (no accidental `^`) | yes | Dependabot lockfile PRs; Orval exact pin `"8.36.0"` confirmed after #606 |
| Documented pins synced | n/a | No documented pin moved |
| Workspace install | yes (#606) | `yarn install` in `ezkey_mobile` after merging #606 onto main |
| Codegen after Orval bump | yes (#606) | `yarn generate:api` → empty generated-client diff |
| Exploratory human | n/a | |
| Mobile `yarn test` (ezkey_mobile) | yes (#606) | 42 suites / 305 tests passed |

## Holds and deferrals

- **#606** Orval mobile — **merged** (closeout 2026-09-26); was held pending cheap-exception generate+unit tests; evidence above.
- **#498**, **#337** — `deferred:later-train` (TypeScript 7); skipped.
- Non-Dependabot out of scope: #611 #610 #565 #539 #538 #347.

## Out of scope this pass

- Deploy
- Merging deferred / non-Dependabot PRs
- Maven / Boot / google-java-format bumps
- Full clean-start / Playwright (document recommendation; leave to next session)

## Merge SHAs (squash onto main)

| PR | Squash SHA (short) |
|----|--------------------|
| #602 | `2a4a191…` (prior turn) |
| #593 | `abbe4f2…` |
| #592 | `99ec5a8…` |
| #591 | `1c98bfe…` |
| #590 | `63374de…` |
| #603 | `60c1fc0…` |
| #604 | `c6bc527…` |
| #607 | `143a13b…` |
| #605 | `8025a6b…` |
| #606 | `bfb5f226` |
