# Dependabot curated campaign — 2026-07-22 pass-1

## Metadata

- **Date:** 2026-07-22
- **Operator:** Marc (+ agent)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** #400, #399, #398, #397, #396, #395, #394, #393, #392, #391, #342, #337 (+ non-Dependabot #347 TS7 idea)
- **Highest accepted tier:** T4

## Lots overview

1. **Already deferred — skip HITL:** `#337`, `#342`, `#347` — TypeScript 7 train; label `deferred:later-train` introduced this pass
2. **Lot A (T1 Maven patches):** `#398` native-maven-plugin, `#396` logback, `#392` tomcat
3. **Lot B (T1 Admin UI patches):** `#400` Playwright, `#399` react-i18next, `#395` react-query, `#393` Vite group
4. **Lot C (T3):** `#394` jackson-2-bom 2.21.5 → 2.22.1
5. **Lot D (T4):** `#397` openapi-generator-maven-plugin (demo-device), `#391` openapi-generator-cli (JS SDK)

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| Deferred | #337, #342, #347 | T4 park | defer | Label + comments + skill/README/`AGENTS.md` contract |
| A | #398, #396, #392 | T1 | merge | #392 conflict after logback merge; rebased then merged |
| B | #400, #399, #395, #393 | T1 | merge | No dedicated pre-merge suite; Playwright at session closeout |
| C | #394 | T3 | merge | `./scripts/build.sh` green on rebased PR branch before merge |
| D | #397, #391 | T4 | merge | Solo-reviewed surfaces (demo-device Maven plugin; JS SDK CLI); operator runs closeout |

## Rationale (short)

### Deferred — TypeScript 7

Standing park for ~months until TS 7.1 / typescript-eslint Node API readiness. Label
`deferred:later-train` so weekly passes list once under skip-HITL and do not re-ask.

### Lot A — Maven T1 patches

Patch-only tooling/runtime (Graal native plugin, Logback, Tomcat). Low blast radius; merge after
mergeability. Tomcat needed a manual rebase onto post-logback `pom.xml`.

### Lot B — Admin UI T1 patches

All patch bumps. Runtime + Vite imply Playwright in session closeout, not a separate pre-merge
gate. Merged individually without conflict.

### Lot C — Jackson 2 BOM minor

T3 surface minor on `jackson-2-bom` (annotations/compat path; JSON engine remains Jackson 3).
Validated with full `./scripts/build.sh` on the rebased Dependabot branch before squash-merge.

### Lot D — OpenAPI generator

Hard-escalator policy: not silent-batched with patches. `#397` touches only `ezkey-demo-device`;
`#391` is JS SDK CLI. Accepted after explicit Go; operator owns build / clean-start / functional /
manual closeout in parallel.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | no checks reported | Empty `statusCheckRollup`; mergeability + local build used as gate |
| `./scripts/build.sh` | yes | Agent: exit 0 on Jackson PR branch before `#394`. Operator: full baseline re-run for session closeout |
| Clean-start stack | yes | Operator closeout |
| Functional tests | yes | Operator closeout |
| Elective functional | n/a | Not required this pass |
| Playwright Admin UI | operator judgment | Lot B (runtime + Vite) recommended; exercised as part of operator closeout / manual checks |
| Exact pin preserved (no accidental `^`) | n/a | No Orval pin touched |
| Documented pins synced (`AGENTS.md`, …) | yes | Deferred-label contract in `AGENTS.md` + skill + dependabot README |
| Workspace install (`npm ls` / Yarn clean) | yes | Covered by operator closeout after Admin UI + JS SDK lockfile merges |
| Codegen after Orval / OpenAPI generator bump | n/a / operator | Generator bumps only (demo-device Maven plugin, JS SDK CLI); no Orval. Regen only if operator workflow required it |
| Exploratory human | yes | Operator manual checks as part of closeout |

**Session status:** closed 2026-07-22 — hygiene pass complete. Highest tier merged: T4 (OpenAPI generator, explicit Go).

## Holds and deferrals

- `#337`, `#342` — open; `deferred:later-train`; comments posted 2026-07-22
- `#347` — open (migration idea PR); same label + comment
- Standing table: `product-docs/global/hygiene/dependabot/README.md`
- Re-evaluate TypeScript 7 train when TS 7.1 / typescript-eslint Node API readiness is evidenced (not weekly)

## Residual risks / next actions

- TypeScript 7 remains intentionally unmerged (~months). No weekly HITL on `deferred:*`.
- Next `dependabot-curated` session: list `deferred:later-train` once under skip-HITL, triage only new Dependabot PRs.
- No GitHub issue — hygiene canon is the campaign note + standing deferral table.

## Out of scope this pass

- Mobile npm Dependabot (none open)
- Closing/ignoring TypeScript 7 via `@dependabot ignore`
