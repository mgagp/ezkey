# Dependabot curated campaign — 2026-09-21 pass-1

## Metadata

- **Date:** 2026-09-21
- **Operator:** Marc (+ agent, autonomous validation mode — standing weekly Dependabot hygiene)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** 2 — #337, #498 (both `deferred:later-train`)
- **Highest accepted tier:** n/a (empty active queue — no merge)
- **GitHub write identity:** GitHub MCP `user-Github` as `mgagp` (admin on `mgagp/ezkey`)

## Lots overview

**Already deferred — skip HITL**

- **#337** (Dependabot): TypeScript 6.0.3 → 7.0.2 in `/ezkey-sdk/javascript` — `deferred:later-train`.
- **#498** (Dependabot): Admin UI TypeScript group bump — `deferred:later-train` (supersedes closed #450).
- Traceability (non-Dependabot): **#347** docs/backlog TypeScript 7 migration idea — same later-train label.

**Active lots:** none. After peeling `deferred:*`, the Dependabot queue is empty. No T1–T4 weekly lots proposed; no inventing I-* / TB-* for routine silence.

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| — | #337, #498 | T4 | defer (already labeled) | TypeScript 7 later-train; skip weekly HITL. |
| — | _(none)_ | — | n/a | Empty active queue after peel. |

No squash-merges this pass.

## Rationale (short)

### Empty active queue
Post-2026-09-15 hygiene (#551 + Orval Admin UI solo), Dependabot has not opened new unlabeled PRs. Standing TS7 deferrals remain parked until TS 7.1 / typescript-eslint Node API readiness — not a weekly decision.

### Java BOM pulse
Root `spring-boot.version` is **4.1.1**. Latest same-minor on Maven Central / endoflife.date is **4.1.1** (2026-08-20). `4.2.0-M1` is next-minor milestone — out of pulse / T4 if ever considered. No Boot hygiene-branch lot.

### Nested pin
`google-java-format.version` **1.36.1** matches Maven Central `latest`/`release`. No bump.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | n/a | Nothing merged |
| Java BOM pulse (`spring-boot.version` vs current same-minor) | yes | `4.1.1` == latest 4.1.x → **none** needed |
| SEC-019 overrides reviewed after Boot bump | n/a | No Boot bump |
| Nested pin: `google-java-format` | yes | `1.36.1` current; no bump |
| `./scripts/build.sh` | n/a | No accepted lots — closeout ladder not required |
| Clean-start stack | n/a | |
| Functional tests | n/a | |
| Elective functional | n/a | |
| Playwright Admin UI | n/a | |
| Exact pin preserved (no accidental `^`) | n/a | No pin/codegen merge |
| Documented pins synced (`AGENTS.md`, …) | n/a | |
| Workspace install (`npm ls` / Yarn clean) | n/a | |
| Codegen after Orval / OpenAPI generator bump | n/a | |
| Exploratory human | n/a | |

Explicit: **no T1–T3 accepted** → session closeout ladder deferred until the next pass that merges bumps. Empty queue is a valid weekly outcome.

## Holds and deferrals

- **#337 / #498** TypeScript 7 — remain `deferred:later-train` (re-evaluate at TS 7.1 / eslint API readiness).
- **#347** migration-idea PR — same train (not Dependabot; listed for traceability).
- No new holds this pass.

## Out of scope this pass

- Docker image tags (operator-owned).
- Re-opening TypeScript 7 or inventing weekly lots from silence.
- Hygiene-branch apply path (default merge-existing unused — nothing to merge).
