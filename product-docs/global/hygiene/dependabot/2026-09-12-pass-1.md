# Dependabot curated campaign — 2026-09-12 pass-1

## Metadata

- **Date:** 2026-09-12
- **Operator:** Marc (+ agent, autonomous validation mode, hygiene-branch exception)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** 12 — #337, #498, #523, #524, #525, #526, #527, #528, #529, #530, #531, #532
- **Highest accepted tier:** T3
- **Apply path:** hygiene branch `cursor/dependabot-hygiene-8f25` (operator asked for one reviewable PR; GitHub merge API is read-only in this runtime)

## Lots overview

**Already deferred — skip HITL**

- **#337**, **#498** (Dependabot) and **#347** (migration-idea PR): TypeScript 7 later-train. Operator confirmed this pass stays deferred while a separate branch evaluates TS 7 readiness.

1. **Lot 1 — Maven tooling** (#525 spotbugs-maven-plugin 4.10.4.0→4.10.4.1, #526 checkstyle 14.0.0→14.1.0, #529 maven-surefire-plugin 3.5.6→3.6.0, #532 maven-compiler-plugin 3.15.0→3.16.0). Blast radius: compile / test / lint tooling.
2. **Lot 2 — Maven runtime** (#531 shedlock 7.9.0→7.10.0). Blast radius: distributed locking for scheduled jobs.
3. **Lot 3 — Admin UI runtime i18n** (#530 i18next 26.4.0→26.4.2, #528 react-i18next 17.0.12→17.0.13). Blast radius: shipped Admin UI strings.
4. **Lot 4 — Admin UI tooling** (#524 eslint 10.9.1→10.10.0 + eslint-plugin-react-refresh 0.5.5→0.5.6, #527 @types/react-dom 19.2.4→19.2.7). Blast radius: lint / types only.
5. **Lot 5 — JS SDK types** (#523 @types/node 26.4.0→26.4.1). Blast radius: SDK dev types.

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| 1 | #525, #526, #529, #532 | T2 (checkstyle/surefire/compiler minors; spotbugs plugin patch) | merge (hygiene branch) | SpotBugs 4.10.4.1 notes Maven reporting `outputEncoding`/`outputDirectory`/`fork` breakage; Ezkey does not set those on the plugin. Failsafe stays at 3.5.6 (no Dependabot PR). |
| 2 | #531 | T3 | merge (hygiene branch) | ShedLock 7.10.0 adds a jOOQ transaction runner unused here; JDBC/Spring provider path unchanged. |
| 3 | #530, #528 | T1 (runtime patches) | merge (hygiene branch) | Admin UI runtime i18n; Playwright because a shipped UI lib moved. |
| 4 | #524, #527 | T2 | merge (hygiene branch) | ESLint minor + types patch. Lockfile kept at Dependabot resolved versions (did not float `@types/react-dom` to 19.3.x). |
| 5 | #523 | T1 | merge (hygiene branch) | SDK `@types/node` patch; TypeScript remains 6.0.3. |
| — | #337, #498, #347 | T4 | defer | TypeScript 7 later-train; already labeled `deferred:later-train`. |

Integrated this pass (hygiene branch): **10** Dependabot PRs (#523–#532). Hygiene PR **#534** landed on `main` at 2026-09-12 14:13 UTC (merged by `app/cursor`).

**Superseded Dependabot closeout** (same day, PAT session — `GH_TOKEN` injected as `mgagp`):

| PR | Outcome |
|----|---------|
| #524, #527, #528, #530 | Dependabot auto-closed after #534 |
| #525, #526, #529, #531, #532 | Commented “already integrated via #534” and closed with the injected PAT |
| #523 | Left open — Dependabot retargeted 26.4.1 → 26.5.0 after 26.4.1 landed; new T1, no CI yet |
| #337, #498, #347 | Still deferred `later-train` (TypeScript 7) |

Note: GitHub MCP write APIs returned 403 with this PAT; `gh` using `GH_TOKEN` succeeded (comment + close).

## Rationale (short)

### Lot 1 — Maven tooling
Compiler 3.16 and Surefire 3.6 are build-tool minors covered by `./scripts/build.sh`. Checkstyle 14.1.0 adds new checks that stay off unless enabled in `checkstyle-config`; existing rules run via `checkstyle:check`. SpotBugs plugin patch is report-only (`java-doctor-curated`), not a build gate.

### Lot 2 — ShedLock
Runtime minor for scheduled audit-chain / expiry / key-promotion locks. Closeout includes clean-start plus a ShedLock table smoke.

### Lot 3 — Admin UI i18n
Patch-level shipped i18n libraries (nested `$t()` replacement-pattern fix; selector-form types). No copy or routing change.

### Lot 4 — Admin UI tooling
ESLint 10.10.0 tightens a few rules (`new-cap`, `no-extra-bind`); `npm run lint` is the gate. `@types/react-dom` stays on 19.2.7 as Dependabot requested.

### Lot 5 — SDK types
Dev types only. TypeScript 7 remains out of this branch.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | n/a | Hygiene-branch apply; agent owns the ladder (autonomous mode) |
| Java BOM pulse (`spring-boot.version` vs current same-minor) | yes | `4.1.1` is still the latest 4.1.x (2026-08-20) → none needed |
| SEC-019 overrides reviewed after Boot bump | n/a | No Boot bump this pass |
| Nested pin: `google-java-format` | yes | `1.36.1`, still current; no bump |
| `./scripts/build.sh` | yes | BUILD SUCCESS (exit 0). Checkstyle 14.1.0: 0 violations. Unit tests excluding `ezkey-tests`: **1097** tests, 0 failures / 0 errors / 0 skipped (189 Surefire suites). Compiler 3.16.0 + Surefire 3.6.0 exercised. |
| Clean-start stack | yes | `./clean-start.sh` BUILD/start success. Admin API health UP; audit HMAC READY. Running `app.jar` contains `shedlock-*-7.10.0.jar`. |
| Functional tests | yes | `mvn test -pl ezkey-tests -P smoke-tests` → **12** tests, 0 failures (enrollment, authentication, admin token). |
| Runtime ShedLock smoke | yes | `ezkey_shedlock` has 8 lock names including AUTH_ATTEMPT_EXPIRY, KEY_PROMOTION, ADMIN_STARTUP_BOOTSTRAP; Actuator `shedLock` UP |
| Elective functional | no | No Rest Assured / protocol test-dep change |
| Playwright Admin UI | yes | **5/5** passed (language switch, 404, Demo Device login, authenticated workflow, elective deny) |
| Admin UI lint / unit | yes | ESLint 10.10.0: 0 errors, 1 pre-existing warning (`alert-detail.tsx` exhaustive-deps). Vitest: 71/71 |
| Exact pin preserved (no accidental `^`) | yes | Orval still `8.25.0` |
| Documented pins synced (`AGENTS.md`, …) | n/a | No documented exact pin moved |
| Workspace install (`npm ls` / Yarn clean) | yes | Admin UI and JS SDK lockfiles refreshed; `npm ls` matches Dependabot versions |
| Codegen after Orval / OpenAPI generator bump | n/a | Orval unchanged |
| Exploratory human | n/a | Not required for this tier |

## Holds and deferrals

- **#337 / #498 / #347** TypeScript 7 — deferred later-train. Separate evaluation branch is in progress; this pass does not touch `typescript` / `typescript-eslint`.
- **#523** `@types/node` 26.4.1 → 26.5.0 — new T1 after #534 landed; left open for the next weekly lot (no CI on the retargeted head yet).
- **maven-failsafe-plugin 3.5.6** — left as-is (no Dependabot PR). Surefire moved to 3.6.0 only.

## Out of scope this pass

- Docker image tags (e.g. `postgres:18-alpine`) — operator-owned, not a Dependabot ecosystem here.
- TypeScript 7 migration.
- Inventing extra version floats beyond the Dependabot PRs (`@types/react-dom` 19.3.x / `@types/node` 26.5.x were rejected).
