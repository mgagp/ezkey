# Dependabot curated campaign — 2026-07-31 pass-1

## Metadata

- **Date:** 2026-07-31
- **Operator:** Marc (+ agent, autonomous validation mode)
- **Skill:** `dependabot-curated`
- **Branch:** `hygiene/dependabot-2026-07-31` (lot commits; Dependabot PRs not squash-merged to `main` yet)
- **Open Dependabot PRs at start:** #426, #425, #424, #423, #422, #421, #420, #419, #418, #417, #416, #342, #337 (+ non-Dependabot #347)
- **Highest accepted tier:** T3

## Lots overview

1. **Already deferred — skip HITL:** `#337`, `#342`, `#347` — TypeScript 7 train (`deferred:later-train`)
2. **Lot A (T1/T2 Maven tooling + JDBC patch):** `#416` checkstyle, `#418` spotless, `#422` postgresql
3. **Lot B (T3 Maven runtime minors):** `#424` jackson-bom 3.2.1, `#417` logback 1.6.1 (+ companion classpath pins)
4. **Lot C (T1/T2 Admin UI):** `#420` eslint, `#425` globals, `#423` vite plugin-react, `#426` tailwindcss
5. **Lot D (T1 SDK):** `#421` @types/node, `#419` openapi-generator-cli patch

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| Deferred | #337, #342, #347 | T4 park | defer | Standing `deferred:later-train`; not touched |
| A | #416, #418, #422 | T1/T2 | apply | Checkstyle 13.9 required config + record Javadoc companion |
| B | #424, #417 | T3 | apply | Jackson 3.2 needs annotations 2.22 + core pin (JsonApplyView) |
| C | #420, #425, #423, #426 | T1/T2 | apply | Admin UI lint + build + Playwright |
| D | #421, #419 | T1 | apply | Generator patch smoke; SDK `tsc` already red on main (TS6 deprecation) |

Decision values for this pass use **apply on hygiene branch** (operator-requested lot commits) rather than immediate per-PR squash-merge to `main`.

## Rationale (short)

### Deferred — TypeScript 7

Standing park. Operator confirmed keep for later train.

### Lot A — Maven tooling / JDBC

Low risk. Checkstyle 13.9 **removed `JavadocStyle`** and tightened **record `JavadocType`** (component `@param` on the type). Companion commits: drop `JavadocStyle`, fix `ClaimedPendingAuth`, sync `AGENTS.md` + `.cursor/rules/javadoc-type-param.mdc`.

### Lot B — Jackson + Logback

T3 surface. Logback 1.6 is drop-in for 1.5.x post-Janino removal. Jackson 3.2.1 initially failed unit tests (`NoClassDefFoundError: JsonApplyView`) because Springdoc still pulled `jackson-annotations` 2.21 and Boot left `tools.jackson-core` behind the forced databind pin. Fixed by explicit dependencyManagement for annotations `2.22` and `jackson-core` `${jackson-bom.version}`.

### Lot C — Admin UI

Tooling + CSS/Vite patches. Validated with lint, production build, and Playwright (including Demo Device login).

### Lot D — JS SDK

Patch-only. `openapi-generator-cli` generate path exercised. Full `npm run compile` still fails on **pre-existing** TS6 `moduleResolution=node10` deprecation (`TS5107`) with typescript `^6.0.3` on main — out of scope for this pass (TS train deferred).

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | no checks reported | Empty checks on Dependabot branches; local ladder used |
| `./scripts/build.sh` | yes | Exit 0 after Jackson alignment + Checkstyle companions |
| Clean-start stack | yes | `ezkey-tests/clean-start.sh` exit 0 |
| Functional tests | yes | `mvn test -pl ezkey-tests` — 146 run, 0 fail, 2 skipped |
| Elective functional | n/a | No Rest Assured / auth-test dep bump |
| Playwright Admin UI | yes | 5/5 passed (`./scripts/run-ui-tests.sh`) |
| Exact pin preserved (no accidental `^`) | n/a | No Orval pin touched |
| Documented pins synced (`AGENTS.md`, …) | yes | Record Javadoc rule sync for Checkstyle 13.9 |
| Workspace install (`npm ls` / Yarn clean) | yes | Admin UI + JS SDK `npm install` / `npm ls` |
| Codegen after Orval / OpenAPI generator bump | partial | Generator CLI patch smoke; SDK `tsc` pre-existing red |
| Exploratory human | n/a | Automation ladder green for accepted tiers |

## Holds and deferrals

- `#337`, `#342`, `#347` — open; `deferred:later-train`
- After hygiene PR merges to `main`, close superseded Dependabot PRs `#416`–`#426` (except deferred) with a comment pointing at this branch/PR

## Out of scope this pass

- TypeScript 7 train
- Fixing JS SDK `TS5107` / `ignoreDeprecations` (pre-existing; belongs with TS6 hygiene or TS7 train)
- Operational churn suite
- Elective / slow functional profiles

## Autonomy note (directive follow-up)

Operator requested full agent autonomy for lotting, apply, and validation evidence (HITL Go/No-Go waived for this session). Skill/AGENTS should document an **autonomous validation mode** for future cold agents — see skill update in the same change set.
