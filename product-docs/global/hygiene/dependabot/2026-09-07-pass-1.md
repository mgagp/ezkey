# Dependabot curated campaign — 2026-09-07 pass-1

## Metadata

- **Date:** 2026-09-07
- **Operator:** Marc (+ agent, autonomous validation mode)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** 15 — #337, #498, #503, #504, #505, #506, #507, #508, #509, #510, #511, #512, #513, #514, #515
- **Highest accepted tier:** T3

## Lots overview

1. **Lot 1 — Java / Maven** (#511 rewrite-spring patch, #504 spotless-maven-plugin patch, #505 jackson-2-bom patch, #513 shedlock 7.7→7.9 minor). Blast radius: build tooling + one runtime lock library.
2. **Lot 2 — Admin UI** (#515 react-router-dom patch, #514 zod 4.4→4.5 minor, #509 eslint group, #512 @vitejs/plugin-react patch). Blast radius: runtime UI (routing/validation) + lint/build tooling.
3. **Lot 3 — Peripheral SDK/CLI** (#506 @types/node dev types, #507 click Python CLI minor). Blast radius: SDK dev types + CLI runtime (outside the Java/Playwright ladder).
4. **Held — OpenAPI generator** (#508 openapi-generator-maven-plugin, #510 openapi-generator-cli). Hard escalator (generator pin policy → codegen).
5. **Held — Actions major** (#503 actions/setup-java 5→6). Major + failing `js-validate` check.
6. **Deferred — TypeScript 7** (#337 SDK already `deferred:later-train`, #498 Admin UI group pulls TS 6→7).

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| 1 | #511, #504, #505, #513 | T3 (shedlock runtime minor; rest tooling/patch) | merge | Distinct pom.xml properties; merged cleanly |
| 2 | #515, #514, #509 | T3 (react-router/zod runtime UI) | merge | Merged cleanly |
| 2 | #512 | T2 (vite plugin patch) | defer | package.json/package-lock.json conflict after #509/#514/#515; needs Dependabot rebase |
| 3 | #506, #507 | T2 | merge | Distinct ecosystems; merged cleanly |
| 4 | #508, #510 | T4 (OpenAPI generator hard escalator) | hold | Requires codegen regen + review; solo review next pass |
| 5 | #503 | T4 (Actions major) | hold | `js-validate` check FAILING; investigate workflow semantics |
| 6 | #498, #337 | T4 (TypeScript 7 disruptor) | defer | Same train as parked #337; recommend `deferred:later-train` on #498 |

Merged this pass: **9** PRs (#511, #504, #505, #513, #515, #514, #509, #506, #507).

## Rationale (short)

### Lot 1 — Java / Maven
rewrite-spring and spotless-maven-plugin are build tooling patches; jackson-2-bom is a patch of an intentional SEC-019 override pin. shedlock 7.7→7.9 is the only runtime-behavioral change (distributed locking for scheduled audit-chain / expiry / key-promotion jobs), so it drives clean-start + scheduled-job smoke.

### Lot 2 — Admin UI
react-router-dom (patch) and zod (minor) are shipped runtime; validated by Playwright. eslint is lint tooling. @vitejs/plugin-react patch (#512) conflicted on the lockfile after the other three merged and was deferred rather than hand-resolving a generated lockfile.

### Lot 3 — Peripheral
@types/node is dev types only; click is a mature Python CLI dep (minor). Low blast radius; not covered by the Java/Playwright ladder, so relied on their own ecosystem scope.

### Held / Deferred
OpenAPI generator bumps are T4 hard escalators (regenerate + verify SDK output) — deferred to a focused pass. actions/setup-java 5→6 has a failing check and major workflow semantics. TypeScript 7 stays on the existing later-train.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | n/a | No blocking checks configured on merged PRs; agent owns the ladder (autonomous mode) |
| Java BOM pulse (`spring-boot.version` vs current same-minor) | yes | `4.1.1` is the latest 4.1.x (2026-08-20) → none needed |
| SEC-019 overrides reviewed after Boot bump | n/a | No Boot bump this pass |
| Nested pin: `google-java-format` | yes | `1.36.1`, unchanged; no bump warranted |
| `./scripts/build.sh` | yes | BUILD SUCCESS on integrated main; all modules 0 failures / 0 errors |
| Clean-start stack | yes | Rebuilt from merged source; all 8 services healthy; audit HMAC integrity READY |
| Functional tests | no | Not necessary — no auth/enrollment/protocol dependency changed; startup + ShedLock smoke is proportionate |
| Elective functional | no | Not necessary — no test-harness dep (e.g. Rest Assured) changed; not a release closeout |
| Playwright Admin UI | yes | 5/5 passed (device-backed login, authenticated workflow, elective deny) |
| Runtime ShedLock smoke | yes | `ezkey_shedlock` shows AUTH_ATTEMPT_EXPIRY / KEY_PROMOTION / ADMIN_STARTUP_BOOTSTRAP locks; no linkage errors |
| Exact pin preserved (no accidental `^`) | yes | Orval still `8.25.0`; no manual `npm install` performed |
| Documented pins synced (`AGENTS.md`, …) | n/a | No documented exact pin moved |
| Workspace install (`npm ls` clean) | yes | `npm ci` reinstalled from merged lockfile without invalid/missing peers |
| Codegen after Orval / OpenAPI generator bump | n/a | Orval unchanged; generator bumps (#508/#510) held |
| Exploratory human | no | Not required for this tier |

## Holds and deferrals

- **#512** @vitejs/plugin-react 6.1.0→6.1.1 — deferred this pass (lockfile conflict after the other Admin UI merges). Recommend `@dependabot rebase` then merge next pass.
- **#508 / #510** OpenAPI generator (maven plugin / cli) — held; T4 codegen review (regenerate SDK client, diff output).
- **#503** actions/setup-java 5→6 — held; failing `js-validate` check + major workflow semantics to review.
- **#498 / #337** TypeScript 7 — deferred (later-train). Recommend applying `deferred:later-train` to #498 so it drops out of weekly lots.

## Continuation (same session) — codegen review #508 / #510 and #503

After the initial 9-PR lot, the operator directed the OpenAPI-generator codegen review and #503.

| PR | Decision | Result |
|----|----------|--------|
| #508 openapi-generator-maven-plugin 7.24→7.25 (demo-device) | merge | Output is build-time (`target/generated-sources`); Demo Device regenerated + compiled under 7.25.0; `build.sh` BUILD SUCCESS |
| #510 openapi-generator-cli 2.40→2.41 (JS SDK) | merge + hardening | Launcher-only; generation verified. Added `openapitools.json` pinning generator to **7.9.0** (was unpinned, floating to latest 7.25.0) |
| #503 actions/setup-java 5→6 (mobile workflow) | merge | Safe: the `android-jvm-unit-tests` job (the setup-java consumer) passed on v6; the failing `js-validate` check is unrelated (see debt below) |

### Pre-existing debt surfaced (NOT caused by these bumps) — recommend dedicated tasks

1. **JS SDK output is stale vs specs.** Committed `generated/`/`dist/` was produced by generator **7.9.0** but predates many endpoints; a fresh generate adds ~5,468 lines (APIKeys, Dashboard, EncryptionKeys, AdminAuthentication, …). Needs a deliberate SDK refresh, not a wrapper bump.
2. **JS SDK `npm run build` broken on the pinned toolchain.** TypeScript 6.0.3 rejects `tsconfig.json` `moduleResolution: "node"` → `TS5107`. Confirmed identical on baseline `main`. Fix: `ignoreDeprecations: "6.0"` or migrate `moduleResolution`.
3. **Mobile `js-validate` CI red (pre-existing).** `yarn validate:ci` fails on a real type error: `app/screens/Home/HomeScreen.tsx(470,76)` — `EnrollmentMetadataRecord.enrollmentProofToken` (`string | undefined`) not assignable to `StoredEnrollment.enrollmentProofToken` (`string`). Independent of the setup-java bump.

### Validation (continuation)

- `./scripts/build.sh` on the codegen-review branch → BUILD SUCCESS (Demo Device compiled with plugin 7.25.0; all modules 0 failures).
- #510 generation executed successfully with the 7.9.0 pin.
- #503: relied on the PR's own green `android-jvm-unit-tests` (setup-java v6) as evidence; `js-validate` red is orthogonal debt.

### Updated open set

- **#518** admin-ui-vite group (Dependabot **rebase of the deferred #512** onto current `main`, now 2 updates) — next pass.
- **#498 / #337** TypeScript 7 — still deferred (later-train).

## Out of scope this pass

- Docker image tags (e.g. `postgres:18-alpine`) — operator-owned, not a Dependabot ecosystem here.
- Merge mechanism note: the GitHub API/`gh` merge path is read-only in the cloud runtime, so the 9 accepted PRs were integrated into `main` via git merge of their Dependabot branches (provenance preserved; GitHub marks them merged) after the local validation ladder passed. Held/deferred PRs remain open.
