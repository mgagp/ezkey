# Dependabot curated campaign — 2026-07-15 pass-2

## Metadata

- **Date:** 2026-07-15
- **Operator:** Marc (+ agent)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** 9 (`#337`, `#342`, `#350`–`#356`)
- **Highest accepted tier:** T4 (crypto/runtime and OpenAPI tooling)

## Lots overview

1. **Lot A — T1 patch low-risk:** `#354` @types/node 26.0.0→26.1.1 (admin-ui), `#353` react-i18next 17.0.8→17.0.9 (admin-ui), `#351` native-maven-plugin 1.1.3→1.1.4
2. **Lot B — T2 tooling minor:** `#352` rewrite-spring 6.34.0→6.35.0
3. **Lot C — T3 admin-ui runtime minor:** `#355` react-hook-form 7.79.0→7.81.0
4. **Lot D — T4 PR-by-PR:** `#356` orval 8.20.0→8.22.0, `#350` tink 1.22.0→1.23.0
5. **Hold T4:** `#342`, `#337` (TypeScript 7 posture)

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| A | #354, #353, #351 | T1 | merge | Merged individually |
| B | #352 | T2 | merge | Merged individually |
| C | #355 | T3 | merge | Merged individually |
| D1 | #356 | T4 | merge | PR-by-PR per HITL |
| D2 | #350 | T4 | merge | Crypto dependency, accepted with reinforced closeout |
| Hold | #342, #337 | T4 | hold | TypeScript 7 held until 7.1 posture, comments posted |

Decision values:

- **merge** — lot approved; each PR merged individually after CI/mergeability gate
- **hold** — leave open for later session or solo review
- **defer** — decline for now with explicit deferral artifact

## Rationale (short)

### Lot A — T1 patch low-risk

Patch-only updates on types/i18n/build plugin. Suitable for fast lane and merged as a batch decision with individual PR merges.

### Lot B — T2 tooling minor

OpenRewrite recipe minor can influence modernization tooling behavior; accepted with closeout baseline required.

### Lot C — T3 admin-ui runtime minor

`react-hook-form` minor touches shipped UI runtime, so treated separately from patch lots and triggers UI smoke in closeout.

### Lot D1 — Orval T4

Orval is in the OpenAPI generation chain and is treated as a known disruptor class in repo policy; merged solo with explicit closeout checks.

### Lot D2 — Tink T4

Crypto runtime dependency (`com.google.crypto.tink`) is a security-sensitive surface. Merged solo after HITL with reinforced functional/UI closeout.

### Hold — TypeScript 7 (`#342`, `#337`)

Project posture remains to wait for TypeScript 7.1 (documented Admin UI context around PR #347). Kept open; hold comments added on both PRs.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | no checks reported | `gh pr checks` returned no checks on Dependabot branches; merges gated via `mergeable=MERGEABLE` + closeout |
| `./scripts/build.sh` | yes | Passed (`BUILD SUCCESS`) |
| Clean-start stack | yes | `ezkey-tests/clean-start.sh` passed; Admin/Auth/Crypto healthy |
| Functional tests | yes | `mvn test -pl ezkey-tests -P smoke-tests` passed (12 tests, 0 fail) |
| Elective functional | no | Not required for this pass |
| Playwright Admin UI | yes | First run failed due missing generated import (`@/generated/admin-api/...`); after `npm run generate:api`, rerun passed (4/4 smoke) |
| Exploratory human | n/a | Not required; automation provided sufficient signal |

## Holds and deferrals

- `#342` — hold comment posted: wait for TypeScript 7.1 posture.
- `#337` — hold comment posted: wait for TypeScript 7.1 posture.
- No new `I-*` opened in this pass; hold rationale preserved in PR comments and this campaign note.

## Out of scope this pass

- Forcing TypeScript 7 migration before 7.1
- Bulk merge without PR-by-PR HITL on T4 items
