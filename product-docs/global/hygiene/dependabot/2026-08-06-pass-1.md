# Dependabot curated campaign — 2026-08-06 pass-1

## Metadata

- **Date:** 2026-08-06
- **Operator:** Marc (+ agent, autonomous validation mode)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** #438, #437, #436, #435, #434, #433, #342, #337
- **Highest accepted tier:** T3

## Lots overview

1. **Already deferred — skip HITL:** `#337`, `#342` — TypeScript 7 train (`deferred:later-train`). Operator also holds ESLint / typescript-eslint connectivity until ~ESLint 7.1 (no open ESLint PR this pass).
2. **Lot A (T2 Maven Rewrite tooling):** `#433` rewrite-maven-plugin 6.44→6.45, `#434` rewrite-spring 6.35→6.36
3. **Lot B (T3 Admin UI Vite/Tailwind toolchain):** `#435` admin-ui-vite group (vite 8.1.5→8.2.0, `@vitejs/plugin-react` 6.0.4→6.0.5), `#436` `@tailwindcss/vite` 4.3.2→4.3.3
4. **Lot C (T3 Admin UI runtime):** `#437` react-hook-form 7.81→7.84, `#438` react + `@types/react` 19.2.7/19.2.17 → 19.2.8/19.2.18

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| Deferred | #337, #342 | T4 park | defer | Standing `deferred:later-train`; not touched |
| A | #433, #434 | T2 | merge | Squash-merged individually; no CI on Dependabot branches |
| B | #435, #436 | T3 | merge | Vite minor + Tailwind patch; Playwright required |
| C | #437, #438 | T3 | merge | Companion `#439` aligned `react-dom` to 19.2.8 |

## Rationale (short)

### Deferred — TypeScript 7 / ESLint 7.1 train

Standing park for `#337` / `#342`. No ESLint major PR open; wait for tooling connectivity (~ESLint 7.1) per operator.

### Lot A — Maven Rewrite

Dev/tooling only. Single-line `pom.xml` bumps; low blast radius.

### Lot B — Vite / Tailwind

Shipping Admin UI toolchain. Vite 8.2.0 is a minor with HMR/build fixes; Tailwind Vite plugin patch. Validated with production build + Playwright.

### Lot C — React runtime + forms

`react-hook-form` minor and React patch. Dependabot `#438` did not bump `react-dom`, leaving `react@19.2.8` / `react-dom@19.2.7`. React 19 hard-fails on version mismatch in Vite dev (Playwright webServer). Companion hygiene PR `#439` aligned `react-dom` to `19.2.8`.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | no checks reported | Empty checks on Dependabot branches; local ladder used |
| `./scripts/build.sh` | yes | Exit 0 (`BUILD SUCCESS`) after merges on `main` |
| Clean-start stack | yes | `ezkey-tests/clean-start.sh` exit 0 |
| Functional tests | yes | `mvn test -pl ezkey-tests` — 146 run, 0 fail, 2 skipped |
| Elective functional | n/a | No Rest Assured / auth-test dep bump |
| Playwright Admin UI | yes | Failed 5/5 on version mismatch; after `#439` **5/5 passed** |
| Exact pin preserved (no accidental `^`) | n/a | No Orval pin touched |
| Documented pins synced (`AGENTS.md`, …) | n/a | No documented exact pin moved |
| Workspace install (`npm ls` / Yarn clean) | yes | Admin UI `npm install`; versions verified (`react`/`react-dom` 19.2.8 after companion) |
| Codegen after Orval / OpenAPI generator bump | n/a | No Orval / OpenAPI generator bump |
| Exploratory human | n/a | Automation ladder green after companion |

## Holds and deferrals

- `#337`, `#342` — open; `deferred:later-train` (TypeScript 7)
- ESLint / typescript-eslint Node API readiness — wait for ~ESLint 7.1; not in this week's open set

## Out of scope this pass

- TypeScript 7 train
- Migrating `vite.config.ts` off `__dirname` (Vite 8.2 warns about future native config loader; non-blocking)
- npm audit high-severity findings unrelated to this bump set
- Operational churn suite
