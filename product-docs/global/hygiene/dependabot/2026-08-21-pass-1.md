# Dependabot curated campaign — 2026-08-21 pass-1

## Metadata

- **Date:** 2026-08-21
- **Operator:** Marc (+ agent; operator-requested Spring Boot patch, not a weekly Dependabot sweep)
- **Skill:** `dependabot-curated` (hygiene-branch exception — no Dependabot PR existed)
- **Open Dependabot PRs at start:** not listed; this pass was a single directed Boot bump
- **Highest accepted tier:** T1 SemVer, closeout run as runtime (T3 ladder)

## Lots overview

1. **Already deferred — skip HITL:** `#337`, `#450`, `#347` (TypeScript 7 train; unchanged).
2. **Lot A (T1 Maven runtime patch):** Spring Boot `4.1.0` → `4.1.1` via root `spring-boot.version`. No Dependabot PR. Hygiene branch `hygiene/spring-boot-4.1.1`.

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| Deferred | #337, #450, #347 | T4 park | defer | Standing `deferred:later-train`; not reopened |
| A | none (manual) | T1 runtime | apply | Operator asked for 4.1.1 + validation |

## Rationale (short)

### Lot A — Spring Boot 4.1.1

Released 2026-08-20. Patch BOM: 98 bug fixes / docs / dependency upgrades. Gradle gRPC opt-in note does not apply (Maven). No source changes required.

Kept Ezkey overrides:

- `tomcat.version` **11.0.25** (Boot 4.1.1 still manages 11.0.24)
- `logback.version` **1.6.1**
- Jackson BOMs **3.2.1** / **2.22.1**
- `postgresql.version` **42.7.13** (matches Boot 4.1.1)

Notable managed upgrades that landed: Spring Framework **7.0.9**, Spring Security **7.1.1**, Spring Data **4.1.1**, Hibernate **7.4.5.Final**.

`THIRD-PARTY.txt` refreshed by the license plugin during `./scripts/build.sh`. Stale assessment docs that mention Boot 4.1.0 as inventory context were left as dated snapshots.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | n/a | No Dependabot PR; hygiene branch |
| `./scripts/build.sh` | yes | Exit 0. Unit tests **1093** run, 0 fail, 0 skipped (core 472, core-security 46, migration 16, admin-api 432, auth-api 37, integration-api 11, crypto-api 33, java SDK 9, demo-app-acme 9, demo-device 28) |
| Clean-start stack | yes | `ezkey-tests/clean-start.sh` exit 0; images resolved Boot 4.1.1. Health `UP`: Admin `9081`, Auth `8085`, Integration `7081` |
| Functional tests | yes | `mvn test -pl ezkey-tests` — **149** run, 0 fail, **2** skipped |
| Elective functional | n/a | No Rest Assured / auth-test dep bump |
| Playwright Admin UI | n/a | Java BOM only; no Admin UI runtime or workflow change |
| Exact pin preserved (no accidental `^`) | n/a | Maven property bump |
| Documented pins synced (`AGENTS.md`, …) | yes | `docs/dependency-posture-admin-and-ui.md` current anchor 4.1.1 |
| Workspace install (`npm ls` / Yarn clean) | n/a | No npm/Yarn surface |
| Codegen after Orval / OpenAPI generator bump | n/a | No Orval / generator bump |
| Exploratory human | n/a | Automation ladder green |

## Holds and deferrals

- `#337`, `#450`, `#347` — TypeScript 7 train; unchanged
- Standalone `ezkey-demo-device` parent bumped to `spring-boot-starter-parent` **4.1.1** in the sibling repo (same session; separate PR).

## Out of scope this pass

- Weekly Dependabot PR triage
- Admin UI / mobile / SDK TypeScript surfaces
- Playwright (no UI contract change)
- OpenAPI spec refresh (no Java DTO / controller change)
