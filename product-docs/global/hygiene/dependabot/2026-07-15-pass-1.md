# Dependabot curated campaign — 2026-07-15 pass-1

## Metadata

- **Date:** 2026-07-15
- **Operator:** Marc (+ agent)
- **Skill:** `dependabot-curated` (first live pass; lane docs introduced in the same session)
- **Open Dependabot PRs at start:** 13 (`#334`–`#346`)
- **Highest accepted tier:** T3 (Admin UI runtime) + T4 Actions (solo-reviewed merge)

## Lots overview

1. **Lot A — Maven T1 tooling patches:** `#340` Rest Assured 6.0.0→6.0.1, `#341` SpotBugs 4.10.2→4.10.3, `#343` SpotBugs Maven plugin 4.10.2.0→4.10.3.0
2. **Lot B — Admin UI T1 patches:** `#344` Vite 8.1.3→8.1.4, `#346` i18next 26.3.4→26.3.6
3. **Lot C — SDK T1:** `#339` `@types/node` 26.1.0→26.1.1
4. **Lot D — Tooling T2 minors:** `#335` Checkstyle 13.7→13.8, `#336` Rewrite Maven plugin, `#338` ESLint 10.6→10.7
5. **Lot E — Admin UI T3 minor:** `#345` lucide-react 1.21→1.24
6. **Lot F — Actions T4 solo:** `#334` `actions/setup-node` 6→7 (mobile unit-test workflow only)
7. **Hold T4:** `#337` SDK TypeScript 6→7; `#342` Admin UI typescript group (includes TypeScript 7.0.2)

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| A | #340, #341, #343 | T1 | merge | #343 conflicted after #341; `@dependabot rebase` then merged |
| B | #344, #346 | T1 | merge | |
| C | #339 | T1 | merge | |
| D | #335, #336, #338 | T2 | merge | Checkstyle / Rewrite / ESLint minors |
| E | #345 | T3 | merge | Rebased after Admin UI lockfile churn, then merged |
| F | #334 | T4 | merge | Solo review: ESM-internal setup-node v7; touches only `ezkey-mobile-unit-tests.yml`; no workflow input changes required |
| Hold | #337, #342 | T4 | hold | TypeScript 7 disruptor; deferred toward Release 7.1 posture; comments left on both PRs |

## Rationale (short)

### Lot A — Maven T1 tooling patches

True third-digit (and SpotBugs plugin quad) bumps limited to test/static-analysis tooling. Low blast radius; batch approved as the fast lane for this weekly pass.

### Lot B — Admin UI T1 patches

Vite patch in the existing Dependabot vite group plus i18next patch. CI on Dependabot branches reported no checks in this environment; mergeability was CLEAN. Session closeout carries Admin UI validation via Playwright (T3 also merged).

### Lot C — SDK T1

`@types/node` patch only — types surface, not runtime SDK behavior.

### Lot D — Tooling T2 minors

Checkstyle and Rewrite can change lint/rewrite behavior; ESLint minor can change lint noise. Accepted as one tooling lot with mandatory `./scripts/build.sh` in closeout.

### Lot E — Admin UI T3 minor

`lucide-react` jumped 1.21→1.24 (minor span). Icons-only but still shipped UI surface — kept separate from T1 patches and triggers Playwright in session closeout.

### Lot F — Actions T4 solo

Major action tag. Reviewed release notes: V7 breaking change is ESM migration inside the action; workflow YAML usages stay the same. Scoped to mobile unit-test workflow. Accepted after solo review rather than silent-batching with patches.

### Hold — TypeScript 7 (`#337`, `#342`)

Both PRs pull TypeScript **7.0.2**. `#342` also bumps `typescript-eslint` 8.63→8.64, but the controlling risk is the TypeScript major. Matches known Release **7.1** deferral posture — do not merge in weekly hygiene.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | no checks reported | Branches showed empty `statusCheckRollup`; mergeability CLEAN / MERGEABLE used as gate |
| `./scripts/build.sh` | yes | Passed (exit 0) |
| Clean-start stack | yes | `ezkey-tests/clean-start.sh` passed (exit 0) |
| Functional tests | yes | `mvn test -pl ezkey-tests` passed (exit 0) |
| Elective functional | yes | `ezkey-tests/scripts/run-elective-tests.sh` passed (exit 0; Rest Assured patch in Lot A) |
| Playwright Admin UI | yes | `ezkey-admin-ui/scripts/run-ui-tests.sh` — 5 passed (smoke + device-backed + elective deny) |
| Exploratory human | n/a | Not required — automation green |

## Holds and deferrals

- `#337` — open; comment posted (`dependabot-curated` hold / Release 7.1)
- `#342` — open; comment posted (group includes TypeScript 7 major)
- No new `I-*` created this pass: deferral already product-known; comments + this note preserve the decision. Promote an `I-*` later if Release 7.1 planning needs a standing backlog anchor.

## Out of scope this pass

- Mobile app npm ecosystem Dependabot (no open PRs this week)
- Creating umbrella squash commits in place of individual Dependabot merges
- Closing/ignoring TypeScript 7 majors via `@dependabot ignore … major version` (left visible on purpose)
