# Dependabot curated campaign — 2026-08-20 pass-1

## Metadata

- **Date:** 2026-08-20
- **Operator:** Marc (+ agent, autonomous validation mode)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** #472, #467, #466, #465, #464, #463, #454, #453, #450, #337
- **Highest accepted tier:** T3

## Lots overview

1. **Already deferred — skip HITL:** `#337` (SDK TypeScript 7, already labeled), `#347` (migration-idea PR), `#450` (Admin UI TypeScript 7 group — labeled `deferred:later-train` this pass; successor of closed `#342`).
2. **Lot A (T1 Maven runtime patch):** `#465` Tomcat 11.0.24 → 11.0.25.
3. **Lot B (T2 Maven tooling):** `#463` Checkstyle 13.9.0 → 13.11.0, `#464` rewrite-maven-plugin 6.45.0 → 6.46.1, `#467` rewrite-spring 6.36.0 → 6.37.0.
4. **Lot C (T2 types / lint):** `#453` SDK `@types/node` 26.1.2 → 26.2.0, `#466` Admin UI `globals` 17.9.0 → 17.11.0.
5. **Lot D (T3 surface):** `#454` springdoc-openapi 3.0.3 → 3.1.0, `#472` `@hookform/resolvers` 5.7.1 → 5.9.1.
6. **Lot E (companion Admin UI pin hygiene, not Dependabot):** caret bumps still showing after `npm install` — Vite 8.2.2, plugin-react 6.1.0, vitest 4.1.11, i18next 26.4.0, react-i18next 17.0.12, lucide-react 1.33.0. TypeScript 7 left on `~6.0.3`.

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| Deferred | #337, #450, #347 | T4 park | defer | Standing `deferred:later-train`; `#450` labeled this pass |
| A | #465 | T1 | merge | Squash-merged; Tomcat embed patch |
| B | #463, #464, #467 | T2 | merge | Checkstyle 13.11: 0 violations on baseline |
| C | #453, #466 | T2 | merge | Types-only + ESLint globals |
| D | #454, #472 | T3 | merge | springdoc + form resolvers; full ladder |
| E | companion branch | T3 | merge via hygiene PR | Post-merge `npm outdated` remainder except TypeScript 7 |

## Rationale (short)

### Deferred — TypeScript 7 train

Operator excluded the TypeScript set. `#337` and `#347` already parked. `#450` is the new Admin UI group bump (6.0.3 → 7.0.2); labeled and commented so weekly lots skip it. Re-evaluate at TS 7.1 / typescript-eslint Node API readiness.

### Lot A — Tomcat patch

Third-digit bump of the embedded runtime. Low blast radius; still counted toward the T3 closeout because the session also accepted surface minors.

### Lot B — Maven tooling

Checkstyle 13.10 notes expanded Javadoc messages and `@since` validation in `NonEmptyAtclauseDescription`; Ezkey's configured ruleset produced **0** violations. Rewrite plugin/recipe minors are build-tooling only.

### Lot C — Types / lint

SDK `@types/node` minor and Admin UI `globals` minor. No shipped runtime.

### Lot D — springdoc + hookform resolvers

springdoc 3.1.0 can change generated OpenAPI JSON (description preservation, Page schema order). Live `./scripts/update-specs.sh` after clean-start produced **no semantic spec diff** (CRLF-only working-copy noise discarded). `@hookform/resolvers` 5.8–5.9 are bugfix minors (array nested errors); Playwright required because it is Admin UI runtime.

### Lot E — Admin UI pin hygiene

After merging Dependabot PRs, `npm install` in `ezkey-admin-ui` still reported caret-range upgrades (and a high `nanoid` advisory via PostCSS). Applied wanted versions, refreshed the lockfile, kept exact pins (`orval@8.24.0`, `react@19.2.8`, `react-dom@19.2.8`, `typescript@~6.0.3`). Vite 8.2.2 pulled `nanoid@3.3.18`; `npm audit` → 0 vulnerabilities. Unit tests, production build, lint, then Playwright on the same closeout stack.

GitHub Actions reported **no checks** on Dependabot branches; local ladder used (same as 2026-08-06).

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | no checks reported | Empty checks on Dependabot branches; local ladder used |
| `./scripts/build.sh` | yes | Exit 0 (`BUILD SUCCESS`); Checkstyle 13.11.0 → 0 violations |
| Clean-start stack | yes | `ezkey-tests/clean-start.sh` exit 0; Admin API health `UP` |
| Functional tests | yes | `mvn test -pl ezkey-tests` — 149 run, 0 fail, 2 skipped |
| Elective functional | n/a | No Rest Assured / auth-test dep bump |
| Playwright Admin UI | yes | **5/5 passed** (`./scripts/run-ui-tests.sh`) |
| Exact pin preserved (no accidental `^`) | yes | `orval@8.24.0`, `react@19.2.8`, `react-dom@19.2.8`, `typescript@~6.0.3` |
| Documented pins synced (`AGENTS.md`, …) | n/a | Orval pin unchanged |
| Workspace install (`npm ls` / Yarn clean) | yes | Admin UI `npm install`; `npm outdated` only TypeScript 7 remains |
| Codegen after Orval / OpenAPI generator bump | n/a | No Orval bump; `npm run generate:api` run for unit/build (Orval 8.24.0). Live spec refresh: no semantic change |
| Exploratory human | n/a | Automation ladder green |

Admin UI companion (Lot E) extra: `npm test` 69/69; `npm run build` (Vite 8.2.2) OK; `npm run lint` 0 errors (1 pre-existing warning in `alert-detail.tsx`).

## Holds and deferrals

- `#337`, `#450` — open Dependabot; `deferred:later-train` (TypeScript 7)
- `#347` — open migration-idea PR; same label
- TypeScript 7 / typescript-eslint Node API readiness — wait for ~TS 7.1; not in this week's merge set

## Out of scope this pass

- TypeScript 7 train (`#337`, `#450`, `#347`)
- Mobile workspace dependency sweep
- Committing OpenAPI files (springdoc 3.1.0 produced no semantic `/api-docs` drift)
