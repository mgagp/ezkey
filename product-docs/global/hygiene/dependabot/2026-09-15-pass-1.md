# Dependabot curated campaign — 2026-09-15 pass-1

## Metadata

- **Date:** 2026-09-15
- **Operator:** Marc (+ agent, autonomous validation mode, hygiene-branch exception)
- **Skill:** `dependabot-curated`
- **Open Dependabot PRs at start:** 12 — #337, #498, #541, #542, #543, #544, #545, #546, #547, #548, #549, #550
- **Highest accepted tier:** T3
- **Apply path:** hygiene branch `cursor/dependabot-hygiene-0abf` (operator asked to take charge of the Dependabot PRs; GitHub merge API is read-only in this runtime)

## Lots overview

**Already deferred — skip HITL**

- **#337**, **#498** (Dependabot): TypeScript 7 later-train (`deferred:later-train`). Standing set also includes migration-idea **#347**.

**Held this pass (T4 hard escalator)**

- **#548** Orval 8.25.0 → 8.32.0 in Admin UI. OpenAPI generator / Orval pin policy — requires regenerate + hook-shape review. Exact pin `8.25.0` kept.

1. **Lot 1 — Maven tooling** (#547 spotless 3.10.1→3.10.2, #545 build-helper 3.6.1→3.6.2, #544 failsafe 3.5.6→3.6.0). Blast radius: format / generated-sources / integration-test plugin.
2. **Lot 2 — Maven runtime** (#541 shedlock 7.10.0→7.10.1, #542 springdoc 3.1.0→3.1.1). Blast radius: scheduled-job locks + OpenAPI UI starter.
3. **Lot 3 — Admin UI shipped + toolchain** (#550 zod 4.5.2→4.6.2, #546 vite 8.2.2→8.3.0). Blast radius: form validation runtime + Vite 8.3 toolchain (Playwright required).
4. **Lot 4 — Types** (#549 Admin UI `@types/node` 26.2.0→26.5.1, #543 SDK `@types/node` 26.5.0→26.5.1). Blast radius: TypeScript types only.

## Decisions

| Lot | PRs | Tier | Decision | Notes |
|-----|-----|------|----------|-------|
| 1 | #547, #545, #544 | T1 / T2 | merge (hygiene branch) | Failsafe aligned with Surefire 3.6.0 from 2026-09-12 pass-1. |
| 2 | #541, #542 | T1 | merge (hygiene branch) | Runtime patches; closeout includes ShedLock smoke. |
| 3 | #550, #546 | T3 | merge (hygiene branch) | Vite 8.3.0 is a maintenance minor (no 8.0-class breaking changes). Lockfile kept at Dependabot resolved versions (`zod@4.6.2`, `vite@8.3.0`). |
| 4 | #549, #543 | T2 / T1 | merge (hygiene branch) | `@types/node` locked at 26.5.1 (did not float to 26.6.x). |
| — | #548 | T4 | hold | Orval pin policy; exact `8.25.0` preserved. |
| — | #337, #498 | T4 | defer | TypeScript 7 later-train; already labeled. |

Integrated this pass (hygiene branch `cursor/dependabot-hygiene-0abf`, PR **#551**): **9** Dependabot PRs (#541–#547, #549, #550). **#548** held. **#337 / #498** remain deferred.

After **#551** lands on `main`, close superseded Dependabot PRs with “already integrated via #551” (this runtime cannot comment/close them).


## Rationale (short)

### Lot 1 — Maven tooling
Spotless 3.10.2 and build-helper 3.6.2 are plugin patches. Failsafe 3.6.0 matches the Surefire 3.6.0 line already on `main`. Covered by `./scripts/build.sh` (Spotless apply + unit tests); failsafe is exercised when integration tests run.

### Lot 2 — ShedLock + springdoc
Patch-level runtime libraries. ShedLock stays on the JDBC/Spring provider path. springdoc 3.1.1 is the OpenAPI UI starter patch — no DTO/controller contract change in this pass, so no spec refresh.

### Lot 3 — Admin UI vite + zod
Zod is shipped form validation. Vite 8.3.0 is the shipping UI toolchain. Playwright is required. A first `npm install` floated `zod` to 4.6.5 and `@types/node` to 26.6.0; those floats were reverted so the lockfile matches Dependabot.

### Lot 4 — `@types/node`
Dev types only. TypeScript remains 6.0.3.

### Held / deferred
Orval 8.32.0 is a T4 hard escalator (codegen + `orval.config.ts` hook-shape history). TypeScript 7 stays on the later-train.

## Validation evidence

| Step | Ran? | Result |
|------|------|--------|
| Dependabot PR CI (per merged PR) | n/a | Hygiene-branch apply; agent owns the ladder (autonomous mode). All in-scope PRs were MERGEABLE / CLEAN. |
| Java BOM pulse (`spring-boot.version` vs current same-minor) | yes | `4.1.1` is still the latest 4.1.x on Maven Central (lastUpdated 2026-08-20). `4.2.0-M1` is next-minor milestone — out of pulse. → none needed |
| SEC-019 overrides reviewed after Boot bump | n/a | No Boot bump this pass |
| Nested pin: `google-java-format` | yes | `1.36.1`, still current on Maven Central; no bump |
| `./scripts/build.sh` | yes | BUILD SUCCESS. Spotless 3.10.2 + Checkstyle 0 violations. Unit tests excluding `ezkey-tests`: **1109** tests, 0 failures / 0 errors / 0 skipped. Failsafe pin 3.6.0; demo-device used `build-helper-maven-plugin:3.6.2`. |
| Clean-start stack | yes | `./clean-start.sh` complete. Admin API `http://localhost:9081/actuator/health` **UP**; audit HMAC **READY**. Running `app.jar` contains `shedlock-*-7.10.1.jar`. |
| Functional tests | yes | `mvn test -pl ezkey-tests -P smoke-tests` → **12** tests, 0 failures (enrollment, authentication, admin token). |
| Runtime ShedLock smoke | yes | `ezkey_shedlock` has ADMIN_STARTUP_BOOTSTRAP, AUDIT_CHAIN_CHECKPOINT, AUTH_ATTEMPT_EXPIRY, KEY_PROMOTION, KEY_ROTATION, REENCRYPTION; Actuator `shedLock` UP |
| Elective functional | n/a | No Rest Assured / protocol test-dep change |
| Playwright Admin UI | yes | **5/5** passed (language switch, 404, Demo Device login, authenticated workflow, elective deny) |
| Admin UI lint / unit | yes | ESLint: 0 errors, 1 pre-existing warning (`alert-detail.tsx` exhaustive-deps). Vitest: 71/71 |
| Exact pin preserved (no accidental `^`) | yes | Orval still `8.25.0` |
| Documented pins synced (`AGENTS.md`, …) | n/a | No documented exact pin moved |
| Workspace install (`npm ls` / Yarn clean) | yes | Admin UI: `zod@4.6.2`, `vite@8.3.0`, `@types/node@26.5.1`, `orval@8.25.0`. SDK lockfile `@types/node@26.5.1`. |
| Codegen after Orval / OpenAPI generator bump | n/a | Orval unchanged (#548 held) |
| Exploratory human | n/a | Not required unless closeout fails |

## Holds and deferrals

- **#548** Orval 8.25.0 → 8.32.0 — hold; T4 codegen/pin policy. Do not merge with weekly lots.
- **#337 / #498** TypeScript 7 — deferred later-train.

## Out of scope this pass

- Docker image tags (e.g. `postgres:18-alpine`) — operator-owned.
- TypeScript 7 migration.
- Inventing extra version floats beyond the Dependabot PRs (`zod` 4.6.5 / `@types/node` 26.6.x were rejected).
- OpenAPI spec refresh (no Java DTO/controller change).
