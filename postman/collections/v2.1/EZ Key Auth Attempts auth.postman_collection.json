{
  "info": {
    "_postman_id": "ee4f0c3e-66f8-4ea8-a990-e37ddfc4598e",
    "name": "EZ Key Auth Attempts auth",
    "description": "Collection for mobile device authentication operations in the Auth API.\n\n## Endpoints\n\n- **Pending**: Check for pending authentication requests (user-initiated polling)\n- **Respond** / **Respond with context**: Submit user's response (approve/deny) with device signature\n\n## Crypto Operations\n\n- **Step 4 (Validate)**: Validates the integration signature over the canonical Pending payload: proofToken|challengeRequired|contextTitle|contextMessage (NFC for context). See docs/AUTH_ATTEMPT_SIGNATURE_PAYLOAD.md.\n- **Step 5 (Sign)**: Device signs the canonical Respond payload: proofToken|accepted. The backend verifies this exact payload.\n\n## Flow and Create/Respond match\n\n- **Create** (no context) → use **6 respond** after steps 1–5.\n- **Create with Context** (Admin collection) → use **6b respond with context** after steps 1–5 (same implementation; naming for clarity).\n\n## Security Notes\n\n- Pending and Respond use canonical payloads (pipe-separated, NFC for context fields).\n- All cryptographic operations use EC P-256 (ECDSA-SHA256).\n\n## Rate Limiting\n\n- Pending: rate limited; user-initiated polling model.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "_exporter_id": "4087"
  },
  "item": [
    {
      "name": "1 crypto generate device proof token",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set(\"device_proofToken\", jsonData.proofToken);",
              "    ",
              "    pm.test(\"Response contains proofToken\", function () {",
              "        pm.expect(jsonData).to.have.property('proofToken');",
              "        pm.expect(jsonData.proofToken).to.be.a('string');",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url_crypto_api}}/api/v1/crypto/prooftoken",
          "host": [
            "{{base_url_crypto_api}}"
          ],
          "path": [
            "api",
            "v1",
            "crypto",
            "prooftoken"
          ]
        },
        "description": "Generates a unique proof token for device authentication. This token is used to prove the device's authenticity when checking for pending authentication attempts.\n\n**Response (200 OK):**\n- `proofToken`: Unique proof token string\n\n**Usage:** This token must be signed with the device's private key and included in the pending request."
      },
      "response": []
    },
    {
      "name": "2 crypto device prooftoken signature",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set(\"device_prooftoken_signature\", jsonData.signature);",
              "    ",
              "    pm.test(\"Response contains signature\", function () {",
              "        pm.expect(jsonData).to.have.property('signature');",
              "        pm.expect(jsonData.signature).to.be.a('string');",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\r\n    \"data\": \"{{device_proofToken}}\",\r\n    \"privateKey\": \"{{device_privateKey}}\"\r\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url_crypto_api}}/api/v1/crypto/sign",
          "host": [
            "{{base_url_crypto_api}}"
          ],
          "path": [
            "api",
            "v1",
            "crypto",
            "sign"
          ]
        },
        "description": "Signs the device proof token with the device's private key. This signature proves the device's authenticity when checking for pending authentication attempts.\n\n**Request Body:**\n- `data`: The device proof token to sign\n- `privateKey`: Device's private key (EC P-256, PKCS#8 format, Base64 encoded)\n\n**Response (200 OK):**\n- `signature`: Cryptographic signature (ECDSA-SHA256, ASN.1 DER encoded, Base64)\n\n**Usage:** The signature is included in the pending request as `deviceProofTokenSigned`."
      },
      "response": []
    },
    {
      "name": "3 pending",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200 or 204\", function () {",
              "    pm.expect([200, 204]).to.include(pm.response.code);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set(\"authAttemptId\", jsonData.authAttemptId);",
              "    pm.environment.set(\"authAttemptProofToken\", jsonData.authAttemptProofToken);",
              "    pm.environment.set(\"authAttemptProofTokenSignedByIntegration\", jsonData.authAttemptProofTokenSignedByIntegration);",
              "    pm.environment.set(\"authAttemptChallengeRequired\", jsonData.authAttemptChallengeRequired);",
              "    // Always set context fields for payload building (step 4); empty string when not provided",
              "    pm.environment.set(\"contextTitle\", jsonData.contextTitle != null ? jsonData.contextTitle : \"\");",
              "    pm.environment.set(\"contextMessage\", jsonData.contextMessage != null ? jsonData.contextMessage : \"\");",
              "    ",
              "    pm.test(\"Response contains required fields\", function () {",
              "        pm.expect(jsonData).to.have.property('authAttemptId');",
              "        pm.expect(jsonData).to.have.property('authAttemptProofToken');",
              "        pm.expect(jsonData).to.have.property('authAttemptProofTokenSignedByIntegration');",
              "        pm.expect(jsonData).to.have.property('authAttemptChallengeRequired');",
              "    });",
              "    ",
              "    ",
              "    // Log context fields for manual inspection",
              "    if (jsonData.contextTitle || jsonData.contextMessage) {",
              "        console.log('--- Contextual Auth ---');",
              "        console.log('contextTitle:', jsonData.contextTitle);",
              "        console.log('contextMessage:', jsonData.contextMessage);",
              "    } else {",
              "        console.log('No contextual metadata on this auth attempt (standard MFA).');",
              "    }",
              "} else if (pm.response.code === 204) {",
              "    pm.test(\"No pending requests\", function () {",
              "        pm.expect(pm.response.code).to.equal(204);",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\r\n    \"enrollmentId\": {{enrollmentId}},\r\n    \"enrollmentProofToken\": \"{{enrollmentProofToken}}\",\r\n    \"deviceProofToken\": \"{{device_proofToken}}\",\r\n    \"deviceProofTokenSigned\": \"{{device_prooftoken_signature}}\"\r\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url}}/api/v1/auth-attempts/pending",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "v1",
            "auth-attempts",
            "pending"
          ]
        },
        "description": "Checks for pending authentication requests for the device's enrollment. This endpoint uses a **user-initiated polling model** - the mobile app polls only when the user explicitly requests to check for pending attempts.\n\n**Security Enhancement:** Uses `enrollmentProofToken` in the request body (not URL path) to prevent enumeration attacks.\n\n**Request Body:**\n- `enrollmentId` (required): Enrollment ID to check for pending attempts\n- `enrollmentProofToken` (required): Cryptographic proof token that authenticates the enrollment\n- `deviceProofToken` (required): Device proof token generated in step 1\n- `deviceProofTokenSigned` (required): Cryptographic signature of device proof token\n\n**Response (200 OK):**\n- `authAttemptId`: Unique identifier of the authentication attempt\n- `authAttemptProofToken`: Authentication proof token containing challenge data\n- `authAttemptProofTokenSignedByIntegration`: Integration-signed proof token for integrity verification\n- `authAttemptChallengeRequired`: Whether additional challenge validation is required\n- `contextTitle` *(optional)*: Short title for the approval request (e.g. `\"Payment Approval\"`). Null if no context was attached.\n- `contextMessage` *(optional)*: Descriptive message explaining the request (e.g. `\"Authorize payment batch #1497 to Acme Corp for $1,400\"`). Null if no context.\n- `contextDetails` *(optional)*: Structured key-value JSON object for display as a table (e.g. `{\"Amount\":\"$1,400.00\",\"Vendor\":\"Acme Corp\"}`). Null if not provided.\n- `contextLevel` *(optional)*: Visual severity — `INFO` (default, blue), `WARNING` (orange), `CRITICAL` (red). Null if no context.\n\n**Response (204 No Content):**\nNo pending authentication attempts found. This is a normal operational state, not an error.\n\n**Status Codes:**\n- 200: Pending authentication attempt found\n- 204: No pending authentication attempts\n- 400: Invalid request or enrollment proof token\n- 429: Rate limit exceeded\n\n**Note:** The mobile app should verify the integration's signature on `authAttemptProofToken` before proceeding. Context fields are all optional — existing integrations that do not send context fields will receive `null` for these fields."
      },
      "response": []
    },
    {
      "name": "4 crypto validate auth prooftoken by integration",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "// Canonical Pending payload: proofToken|challengeRequired|contextTitle|contextMessage (NFC for context). See AUTH_ATTEMPT_SIGNATURE_PAYLOAD.md",
              "var pt = pm.environment.get(\"authAttemptProofToken\");",
              "var cr = pm.environment.get(\"authAttemptChallengeRequired\");",
              "var ct = pm.environment.get(\"contextTitle\") || \"\";",
              "var cm = pm.environment.get(\"contextMessage\") || \"\";",
              "if (typeof ct === 'string' && ct.normalize) ct = ct.normalize('NFC');",
              "if (typeof cm === 'string' && cm.normalize) cm = cm.normalize('NFC');",
              "var payload = pt + \"|\" + cr + \"|\" + ct + \"|\" + cm;",
              "pm.environment.set(\"pendingPayload\", payload);"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        },
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set(\"valid\", jsonData.valid);",
              "    ",
              "    pm.test(\"Response contains valid field\", function () {",
              "        pm.expect(jsonData).to.have.property('valid');",
              "        pm.expect(jsonData.valid).to.be.a('boolean');",
              "    });",
              "    ",
              "    pm.test(\"Signature is valid\", function () {",
              "        pm.expect(jsonData.valid).to.be.true;",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\r\n    \"data\": \"{{pendingPayload}}\",\r\n    \"signature\": \"{{authAttemptProofTokenSignedByIntegration}}\",\r\n    \"publicKey\": \"{{integrationPublicKey}}\"\r\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url_crypto_api}}/api/v1/crypto/validate",
          "host": [
            "{{base_url_crypto_api}}"
          ],
          "path": [
            "api",
            "v1",
            "crypto",
            "validate"
          ]
        },
        "description": "Validates the integration's signature on the Pending payload. The integration signs the canonical payload proofToken|challengeRequired|contextTitle|contextMessage (see docs/AUTH_ATTEMPT_SIGNATURE_PAYLOAD.md). A pre-request script builds this payload from the Pending response (with NFC for context fields).\n\n**Request Body:**\n- `data`: The full Pending payload (built by pre-request script)\n- `signature`: Integration's signature (authAttemptProofTokenSignedByIntegration)\n- `publicKey`: Integration's public key\n\n**Response (200 OK):**\n- `valid`: Boolean indicating whether the signature is valid\n\n**Security Note:** The mobile app must verify this signature before displaying the authentication request to the user."
      },
      "response": []
    },
    {
      "name": "5 crypto sign authAttempt prooftoken by device",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "// Canonical Respond payload: proofToken|accepted. See AUTH_ATTEMPT_SIGNATURE_PAYLOAD.md",
              "var pt = pm.environment.get(\"authAttemptProofToken\");",
              "// Resolve from collection or environment; normalize string (trim, lowercase); default true when unset",
              "var acc = pm.variables.get(\"authAttemptAccepted\");",
              "var accepted = true;",
              "if (acc !== undefined && acc !== null && acc !== \"\") {",
              "    var s = String(acc).trim().toLowerCase();",
              "    accepted = (s === \"true\" || s === \"1\");",
              "}",
              "var payload = pt + \"|\" + (accepted ? \"true\" : \"false\");",
              "pm.environment.set(\"respondPayload\", payload);"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        },
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set(\"authAttemptProofTokenSignedByDevice\", jsonData.signature);",
              "    ",
              "    pm.test(\"Response contains signature\", function () {",
              "        pm.expect(jsonData).to.have.property('signature');",
              "        pm.expect(jsonData.signature).to.be.a('string');",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\r\n    \"data\": \"{{respondPayload}}\",\r\n    \"privateKey\": \"{{device_privateKey}}\"\r\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url_crypto_api}}/api/v1/crypto/sign",
          "host": [
            "{{base_url_crypto_api}}"
          ],
          "path": [
            "api",
            "v1",
            "crypto",
            "sign"
          ]
        },
        "description": "Signs the canonical Respond payload (proofToken|accepted) with the device's private key. The backend expects the device to sign exactly this payload (see docs/AUTH_ATTEMPT_SIGNATURE_PAYLOAD.md). A pre-request script builds respondPayload from authAttemptProofToken and authAttemptAccepted (default true).\n\n**Request Body:**\n- `data`: The Respond payload (respondPayload from pre-request script)\n- `privateKey`: Device's private key\n\n**Response (200 OK):**\n- `signature`: Used in the respond request as authAttemptProofTokenSignedByDevice\n\n**Match with Create:** Use this request after any Create (with or without context). For \"Create with Context\" you may use \"6b respond with context\" for clarity; the payload and behaviour are the same."
      },
      "response": []
    },
    {
      "name": "6 respond",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    ",
              "    pm.test(\"Response contains required fields\", function () {",
              "        pm.expect(jsonData).to.have.property('result');",
              "        pm.expect(jsonData).to.have.property('message');",
              "    });",
              "    ",
              "    pm.test(\"Result is valid\", function () {",
              "        var validResults = ['APPROVED', 'DENIED', 'FAILED', 'EXPIRED'];",
              "        pm.expect(validResults).to.include(jsonData.result);",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\r\n    \"authAttemptId\": {{authAttemptId}},\r\n    \"authAttemptAccepted\": {{authAttemptAccepted}},\r\n    \"authAttemptProofTokenSignedByDevice\": \"{{authAttemptProofTokenSignedByDevice}}\",\r\n    \"authAttemptChallengeResponse\": {{authAttemptChallengeResponse}}\r\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url}}/api/v1/auth-attempts/respond",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "v1",
            "auth-attempts",
            "respond"
          ]
        },
        "description": "Submits the mobile device's response when the auth attempt was created **without** context (standard Create or Create with API Key / User Identifier). Use after Pending and steps 4–5. The device signature (step 5) must be over the canonical payload proofToken|accepted; see docs/AUTH_ATTEMPT_SIGNATURE_PAYLOAD.md.\n\n**Request Body:**\n- `authAttemptId` (required), `authAttemptAccepted` (required), `authAttemptProofTokenSignedByDevice` (required), `authAttemptChallengeResponse` (0 or challenge code)\n\n**Match:** Create (no context) → use this Respond."
      },
      "response": []
    },
    {
      "name": "6b respond with context",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test(\"Status code is 200\", function () {",
              "    pm.response.to.have.status(200);",
              "});",
              "",
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    ",
              "    pm.test(\"Response contains required fields\", function () {",
              "        pm.expect(jsonData).to.have.property('result');",
              "        pm.expect(jsonData).to.have.property('message');",
              "    });",
              "    ",
              "    pm.test(\"Result is valid\", function () {",
              "        var validResults = ['APPROVED', 'DENIED', 'FAILED', 'EXPIRED'];",
              "        pm.expect(validResults).to.include(jsonData.result);",
              "    });",
              "}"
            ],
            "type": "text/javascript",
            "packages": {},
            "requests": {}
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [],
        "body": {
          "mode": "raw",
          "raw": "{\r\n    \"authAttemptId\": {{authAttemptId}},\r\n    \"authAttemptAccepted\": {{authAttemptAccepted}},\r\n    \"authAttemptProofTokenSignedByDevice\": \"{{authAttemptProofTokenSignedByDevice}}\",\r\n    \"authAttemptChallengeResponse\": {{authAttemptChallengeResponse}}\r\n}",
          "options": {
            "raw": {
              "language": "json"
            }
          }
        },
        "url": {
          "raw": "{{base_url}}/api/v1/auth-attempts/respond",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "v1",
            "auth-attempts",
            "respond"
          ]
        },
        "description": "Same as \"6 respond\"; use this when the auth attempt was created **with context** (Create with Context in the Admin collection). The backend does not include context in the Respond payload — the device still signs proofToken|accepted. This request is a duplicate for flow clarity: Create with Context → Pending (with contextTitle/contextMessage) → steps 4–5 → **6b respond with context**."
      },
      "response": []
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "packages": {},
        "exec": [
          ""
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "packages": {},
        "exec": [
          ""
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "authAttemptChallengeResponse",
      "value": "0",
      "description": "Challenge response (0 if no challenge required, or challenge code if required)"
    },
    {
      "key": "authAttemptAccepted",
      "value": "true",
      "description": "User decision for Respond: true = approve, false = deny. Used to build the device signature payload (proofToken|accepted)."
    }
  ]
}

