# Postman collections (historical leftover)

This tree is **not** the exploratory source of truth.

Living API exploration lives in [`bruno/`](../bruno/) (`bruno/README.md`). Do not add or refresh
Postman collections, and do not point new docs or agents at this folder as canon.

Git history is the archive if a collection must be compared. A later hygiene pass may delete this
tree once no remaining references depend on the path.
