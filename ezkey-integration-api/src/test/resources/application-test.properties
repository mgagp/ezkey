# Integration API unit/integration tests — permissive defaults.
ezkey.api-key.rate-limit.enabled=false

# Avoid coupling auth-attempt tests to checkpoint heartbeat supervision.
ezkey.audit.chain.heartbeat.enabled=false
