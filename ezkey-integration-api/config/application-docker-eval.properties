# =============================================================================
# Docker EVAL runtime profile (opt-in) — Integration API
# =============================================================================
# Product: EZKEY_RUNTIME_PROFILE=eval / clean-start --runtime=eval
# Default (integrity) does not load this file.
#
# Hard coupling (★): Admin checkpoints OFF under eval ⇒ heartbeat must be OFF here
# (otherwise Integration gated POSTs fail-close with 503 after grace).
# MFA crypto + HMAC audit write stay on (docker defaults).
# =============================================================================

# --- OFF together with Admin checkpoints (★) ---
ezkey.audit.chain.heartbeat.enabled=false
ezkey.audit.chain.heartbeat.required=false

# Chain checkpoint scheduler remains Admin-only (already false on docker)
ezkey.audit.chain.enabled=false
