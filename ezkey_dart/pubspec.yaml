name: ezkey_dart
description: Pure Dart cryptographic helpers and live-stack bridge utilities for EZKey.
version: 0.1.0

executables:
  ezkey_dart: ezkey_dart
  ezkey_auth_runner: ezkey_auth_runner

environment:
  sdk: '>=3.7.0 <4.0.0'
dependencies:
  args: ^2.7.0
  convert: ^3.1.2
  cryptography: ^2.9.0
  http: ^1.6.0
  pointycastle: ^4.0.0
  unorm_dart: ^0.3.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
