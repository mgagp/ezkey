"use strict";
/* tslint:disable */
/* eslint-disable */
/**
 * Ezkey Admin API
 * Administration API for Ezkey - Open Source Cryptographic MFA Platform  This API enables administrative management of Ezkey\'s main entities: - **Integrations**: Applications or systems protected by MFA - **Enrollments**: Associations between users, devices and integrations - **Auth Attempts**: MFA authentication attempts - **Admin Management**: Administrator authentication,                         enrollment recovery, and admin operations  Ezkey is intentionally distinct from FIDO2/WebAuthn and follows its own cryptographic MFA model. The API uses REST conventions and DTOs for all requests and responses.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: info@ezkey.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.EncryptionKeysApi = void 0;
const runtime = __importStar(require("../runtime"));
/**
 *
 */
class EncryptionKeysApi extends runtime.BaseAPI {
    /**
     * Creates re-encryption batches for all old keys without processing them. Batches will be processed by the scheduled job. Global Admin only.
     * Create re-encryption batches
     */
    async createBatchesRaw(initOverrides) {
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/reencrypt/create-batches`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Creates re-encryption batches for all old keys without processing them. Batches will be processed by the scheduled job. Global Admin only.
     * Create re-encryption batches
     */
    async createBatches(initOverrides) {
        const response = await this.createBatchesRaw(initOverrides);
        return await response.value();
    }
    /**
     * Returns details for a specific encryption key. Global Admin only.
     * Get encryption key by ID
     */
    async getKeyRaw(requestParameters, initOverrides) {
        if (requestParameters['keyId'] == null) {
            throw new runtime.RequiredError('keyId', 'Required parameter "keyId" was null or undefined when calling getKey().');
        }
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/{keyId}`.replace(`{${"keyId"}}`, encodeURIComponent(String(requestParameters['keyId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Returns details for a specific encryption key. Global Admin only.
     * Get encryption key by ID
     */
    async getKey(requestParameters, initOverrides) {
        const response = await this.getKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Returns the current primary encryption key used for new encryption operations. Global Admin only.
     * Get primary encryption key
     */
    async getPrimaryKeyRaw(initOverrides) {
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/primary`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Returns the current primary encryption key used for new encryption operations. Global Admin only.
     * Get primary encryption key
     */
    async getPrimaryKey(initOverrides) {
        const response = await this.getPrimaryKeyRaw(initOverrides);
        return await response.value();
    }
    /**
     * Returns re-encryption batches with pagination and optional filters. Use page, size, sort (default sort createdAt,DESC). Optional filters: status (PENDING, IN_PROGRESS, COMPLETED, FAILED, PAUSED), targetTable, targetColumn, oldKeyId, newKeyId, createdAfter, createdBefore (ISO-8601, inclusive bounds on createdAt). Sortable: batchId, status, targetTable, targetColumn, createdAt, startedAt, completedAt, progressPct, recordsTotal, recordsDone, oldKey.keyId, newKey.keyId. Global Admin only.
     * List re-encryption batches
     */
    async listBatchesRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }
        if (requestParameters['targetTable'] != null) {
            queryParameters['targetTable'] = requestParameters['targetTable'];
        }
        if (requestParameters['targetColumn'] != null) {
            queryParameters['targetColumn'] = requestParameters['targetColumn'];
        }
        if (requestParameters['oldKeyId'] != null) {
            queryParameters['oldKeyId'] = requestParameters['oldKeyId'];
        }
        if (requestParameters['newKeyId'] != null) {
            queryParameters['newKeyId'] = requestParameters['newKeyId'];
        }
        if (requestParameters['createdAfter'] != null) {
            queryParameters['createdAfter'] = requestParameters['createdAfter'];
        }
        if (requestParameters['createdBefore'] != null) {
            queryParameters['createdBefore'] = requestParameters['createdBefore'];
        }
        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }
        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }
        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/reencryption-batches`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Returns re-encryption batches with pagination and optional filters. Use page, size, sort (default sort createdAt,DESC). Optional filters: status (PENDING, IN_PROGRESS, COMPLETED, FAILED, PAUSED), targetTable, targetColumn, oldKeyId, newKeyId, createdAfter, createdBefore (ISO-8601, inclusive bounds on createdAt). Sortable: batchId, status, targetTable, targetColumn, createdAt, startedAt, completedAt, progressPct, recordsTotal, recordsDone, oldKey.keyId, newKey.keyId. Global Admin only.
     * List re-encryption batches
     */
    async listBatches(requestParameters = {}, initOverrides) {
        const response = await this.listBatchesRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Returns encryption keys with pagination and optional keyStatus filter. Use page, size, sort for pagination. Optional keyStatus: PRIMARY, ENABLED, DISABLED, PENDING. Sortable: keyId, keyStatus, algorithm, introducedAt, promotedPrimaryAt, disabledAt, recordsEncrypted, recordsReencrypted, createdBy, createdAt. Global Admin only.
     * List encryption keys
     */
    async listKeysRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['keyStatus'] != null) {
            queryParameters['keyStatus'] = requestParameters['keyStatus'];
        }
        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }
        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }
        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Returns encryption keys with pagination and optional keyStatus filter. Use page, size, sort for pagination. Optional keyStatus: PRIMARY, ENABLED, DISABLED, PENDING. Sortable: keyId, keyStatus, algorithm, introducedAt, promotedPrimaryAt, disabledAt, recordsEncrypted, recordsReencrypted, createdBy, createdAt. Global Admin only.
     * List encryption keys
     */
    async listKeys(requestParameters = {}, initOverrides) {
        const response = await this.listKeysRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Resumes processing of a failed or paused re-encryption batch. Global Admin only.
     * Resume re-encryption batch
     */
    async resumeBatchRaw(requestParameters, initOverrides) {
        if (requestParameters['batchId'] == null) {
            throw new runtime.RequiredError('batchId', 'Required parameter "batchId" was null or undefined when calling resumeBatch().');
        }
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/reencryption-batches/{batchId}/resume`.replace(`{${"batchId"}}`, encodeURIComponent(String(requestParameters['batchId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Resumes processing of a failed or paused re-encryption batch. Global Admin only.
     * Resume re-encryption batch
     */
    async resumeBatch(requestParameters, initOverrides) {
        const response = await this.resumeBatchRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Introduces a new encryption key (typically PENDING until the sync window elapses, then promoted to PRIMARY). Global Admin only.
     * Manually trigger key rotation
     */
    async rotateKeyRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['reason'] != null) {
            queryParameters['reason'] = requestParameters['reason'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/rotate`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Introduces a new encryption key (typically PENDING until the sync window elapses, then promoted to PRIMARY). Global Admin only.
     * Manually trigger key rotation
     */
    async rotateKey(requestParameters = {}, initOverrides) {
        const response = await this.rotateKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Accepts a manual full re-encryption request: creates batches for old keys and enqueues background processing. Returns immediately; use the batches list for progress. Global Admin only.
     * Trigger full re-encryption
     */
    async triggerFullReencryptionRaw(initOverrides) {
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/reencrypt/trigger`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Accepts a manual full re-encryption request: creates batches for old keys and enqueues background processing. Returns immediately; use the batches list for progress. Global Admin only.
     * Trigger full re-encryption
     */
    async triggerFullReencryption(initOverrides) {
        const response = await this.triggerFullReencryptionRaw(initOverrides);
        return await response.value();
    }
    /**
     * Accepts re-encryption for a specific old key: creates batches and enqueues background processing. The key must not be PRIMARY. Returns immediately. Global Admin only.
     * Trigger re-encryption for specific key
     */
    async triggerReencryptionForKeyRaw(requestParameters, initOverrides) {
        if (requestParameters['keyId'] == null) {
            throw new runtime.RequiredError('keyId', 'Required parameter "keyId" was null or undefined when calling triggerReencryptionForKey().');
        }
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/encryption-keys/{keyId}/reencrypt`.replace(`{${"keyId"}}`, encodeURIComponent(String(requestParameters['keyId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Accepts re-encryption for a specific old key: creates batches and enqueues background processing. The key must not be PRIMARY. Returns immediately. Global Admin only.
     * Trigger re-encryption for specific key
     */
    async triggerReencryptionForKey(requestParameters, initOverrides) {
        const response = await this.triggerReencryptionForKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }
}
exports.EncryptionKeysApi = EncryptionKeysApi;
//# sourceMappingURL=EncryptionKeysApi.js.map