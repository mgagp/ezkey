"use strict";
/* tslint:disable */
/* eslint-disable */
/**
 * Ezkey Admin API
 * Administration API for Ezkey - Open Source Cryptographic MFA Platform  This API enables administrative management of Ezkey\'s main entities: - **Integrations**: Applications or systems protected by MFA - **Enrollments**: Associations between users, devices and integrations - **Auth Attempts**: MFA authentication attempts - **Admin Management**: Administrator authentication,                         enrollment recovery, and admin operations  Ezkey is intentionally distinct from FIDO2/WebAuthn and follows its own cryptographic MFA model. The API uses REST conventions and DTOs for all requests and responses.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: info@ezkey.org
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetChainCheckpointsCheckpointTypeEnum = exports.GetAuditLogsApiNameEnum = exports.GetAuditLogsEventStatusEnum = exports.GetAuditLogsEventTypeFamilyEnum = exports.GetAuditLogsEventTypeEnum = exports.AuditLogsApi = void 0;
const runtime = __importStar(require("../runtime"));
/**
 *
 */
class AuditLogsApi extends runtime.BaseAPI {
    /**
     * Verifies chain checkpoint integrity by recomputing entry digests and validating chain linkage. Detects entry insertion, deletion, reordering, checkpoint tampering, and undeclared temporal gaps (missing checkpoints between consecutive windows or uncovered leading/trailing periods in the requested range). Global Admin only.  **Range handling:** The requested from/to may extend before the first checkpoint or after the last in the database. Such periods are not reported as undeclared gaps (they are before/after \"EZKey time\"). Only real gaps within the system\'s checkpoint extent are reported. The response fields effectiveFrom and effectiveTo indicate the range actually used for boundary gap reporting (clamped to coverageStart/coverageEnd when the request extended beyond the first/last checkpoint).
     * Verify audit chain checkpoint integrity
     */
    async checkChainIntegrityRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }
        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/chain-integrity`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Verifies chain checkpoint integrity by recomputing entry digests and validating chain linkage. Detects entry insertion, deletion, reordering, checkpoint tampering, and undeclared temporal gaps (missing checkpoints between consecutive windows or uncovered leading/trailing periods in the requested range). Global Admin only.  **Range handling:** The requested from/to may extend before the first checkpoint or after the last in the database. Such periods are not reported as undeclared gaps (they are before/after \"EZKey time\"). Only real gaps within the system\'s checkpoint extent are reported. The response fields effectiveFrom and effectiveTo indicate the range actually used for boundary gap reporting (clamped to coverageStart/coverageEnd when the request extended beyond the first/last checkpoint).
     * Verify audit chain checkpoint integrity
     */
    async checkChainIntegrity(requestParameters = {}, initOverrides) {
        const response = await this.checkChainIntegrityRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Recomputes HMAC-SHA256 signatures for audit log entries in the specified date range and reports any tampered or unsigned entries. Global Admin only.
     * Verify audit log integrity
     */
    async checkIntegrityRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }
        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/integrity-check`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Recomputes HMAC-SHA256 signatures for audit log entries in the specified date range and reports any tampered or unsigned entries. Global Admin only.
     * Verify audit log integrity
     */
    async checkIntegrity(requestParameters = {}, initOverrides) {
        const response = await this.checkIntegrityRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Recomputes the HMAC-SHA256 signature for a single audit log entry and compares it to the stored value. Returns OK if the entry is intact, INTEGRITY_VIOLATION_DETECTED if modified, UNSIGNED if not signed, or NOT_FOUND if the ID does not exist. Global Admin only.
     * Verify integrity of a single audit log entry
     */
    async checkSingleEntryIntegrityRaw(requestParameters, initOverrides) {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError('id', 'Required parameter "id" was null or undefined when calling checkSingleEntryIntegrity().');
        }
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/{id}/integrity-check`.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Recomputes the HMAC-SHA256 signature for a single audit log entry and compares it to the stored value. Returns OK if the entry is intact, INTEGRITY_VIOLATION_DETECTED if modified, UNSIGNED if not signed, or NOT_FOUND if the ID does not exist. Global Admin only.
     * Verify integrity of a single audit log entry
     */
    async checkSingleEntryIntegrity(requestParameters, initOverrides) {
        const response = await this.checkSingleEntryIntegrityRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Marks a sealed checkpoint tranche as EXPORTED after an external archival workflow has successfully persisted the corresponding bundle. Supports both timestamp range and checkpoint ID identification modes, mirroring seal-archive. Global Admin only.
     * Confirm externally archived audit tranche
     */
    async confirmArchivedRaw(requestParameters, initOverrides) {
        if (requestParameters['archiveConfirmArchivedRequest'] == null) {
            throw new runtime.RequiredError('archiveConfirmArchivedRequest', 'Required parameter "archiveConfirmArchivedRequest" was null or undefined when calling confirmArchived().');
        }
        const queryParameters = {};
        const headerParameters = {};
        headerParameters['Content-Type'] = 'application/json';
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/confirm-archived`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['archiveConfirmArchivedRequest'],
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Marks a sealed checkpoint tranche as EXPORTED after an external archival workflow has successfully persisted the corresponding bundle. Supports both timestamp range and checkpoint ID identification modes, mirroring seal-archive. Global Admin only.
     * Confirm externally archived audit tranche
     */
    async confirmArchived(requestParameters, initOverrides) {
        const response = await this.confirmArchivedRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Creates a single GAP_DECLARATION checkpoint covering the specified period and signs it into the chain with the admin\'s justification. Use when the system was offline longer than the scheduler lookback window. Must be called before the scheduler fills the gap with regular checkpoints. Global Admin only.  **Gap start -- two alternative modes:** - **Timestamp mode**: provide `gapStart` as an ISO-8601 timestamp (the moment the system went offline). - **Anchor checkpoint mode**: provide `anchorCheckpointId`, the `checkpoint_id` of the last checkpoint recorded before the downtime. The service derives `gapStart = anchorCheckpoint.window_end` automatically. Ergonomic when working directly with the database -- look up the last checkpoint ID before the gap and pass it directly, no timestamp extraction needed.  `gapEnd` is always a timestamp when provided. In anchor checkpoint mode it is **optional**: if omitted, the service auto-derives it as the `window_start` of the first checkpoint that exists after the anchor (the boundary between the undeclared gap and the scheduler\'s catch-up checkpoints). If no such checkpoint exists yet, falls back to the start of the current 5-minute window. Exactly one of `gapStart` or `anchorCheckpointId` must be provided.
     * Declare a downtime gap in the audit chain
     */
    async declareGapRaw(requestParameters, initOverrides) {
        if (requestParameters['gapDeclarationRequest'] == null) {
            throw new runtime.RequiredError('gapDeclarationRequest', 'Required parameter "gapDeclarationRequest" was null or undefined when calling declareGap().');
        }
        const queryParameters = {};
        const headerParameters = {};
        headerParameters['Content-Type'] = 'application/json';
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/declare-gap`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['gapDeclarationRequest'],
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Creates a single GAP_DECLARATION checkpoint covering the specified period and signs it into the chain with the admin\'s justification. Use when the system was offline longer than the scheduler lookback window. Must be called before the scheduler fills the gap with regular checkpoints. Global Admin only.  **Gap start -- two alternative modes:** - **Timestamp mode**: provide `gapStart` as an ISO-8601 timestamp (the moment the system went offline). - **Anchor checkpoint mode**: provide `anchorCheckpointId`, the `checkpoint_id` of the last checkpoint recorded before the downtime. The service derives `gapStart = anchorCheckpoint.window_end` automatically. Ergonomic when working directly with the database -- look up the last checkpoint ID before the gap and pass it directly, no timestamp extraction needed.  `gapEnd` is always a timestamp when provided. In anchor checkpoint mode it is **optional**: if omitted, the service auto-derives it as the `window_start` of the first checkpoint that exists after the anchor (the boundary between the undeclared gap and the scheduler\'s catch-up checkpoints). If no such checkpoint exists yet, falls back to the start of the current 5-minute window. Exactly one of `gapStart` or `anchorCheckpointId` must be provided.
     * Declare a downtime gap in the audit chain
     */
    async declareGap(requestParameters, initOverrides) {
        const response = await this.declareGapRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Supplies justification and classified root cause for an incident in RECOVERED_PENDING_DECLARATION. Global Admin only.
     * Declare an audit-chain heartbeat incident closed
     */
    async declareLifecycleIncidentRaw(requestParameters, initOverrides) {
        if (requestParameters['incidentId'] == null) {
            throw new runtime.RequiredError('incidentId', 'Required parameter "incidentId" was null or undefined when calling declareLifecycleIncident().');
        }
        if (requestParameters['declareAuditChainIncidentRequest'] == null) {
            throw new runtime.RequiredError('declareAuditChainIncidentRequest', 'Required parameter "declareAuditChainIncidentRequest" was null or undefined when calling declareLifecycleIncident().');
        }
        const queryParameters = {};
        const headerParameters = {};
        headerParameters['Content-Type'] = 'application/json';
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/incidents/{incidentId}/declare`.replace(`{${"incidentId"}}`, encodeURIComponent(String(requestParameters['incidentId']))),
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['declareAuditChainIncidentRequest'],
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Supplies justification and classified root cause for an incident in RECOVERED_PENDING_DECLARATION. Global Admin only.
     * Declare an audit-chain heartbeat incident closed
     */
    async declareLifecycleIncident(requestParameters, initOverrides) {
        const response = await this.declareLifecycleIncidentRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Returns the current backend lifecycle summary used by future archival automation. This endpoint does not materialize an export bundle; it reports whether external archival is enabled and which sealed checkpoint tranche currently awaits confirmation.
     * Get archive eligibility summary
     */
    async getArchiveEligibilityRaw(initOverrides) {
        const queryParameters = {};
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/archive-eligibility`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Returns the current backend lifecycle summary used by future archival automation. This endpoint does not materialize an export bundle; it reports whether external archival is enabled and which sealed checkpoint tranche currently awaits confirmation.
     * Get archive eligibility summary
     */
    async getArchiveEligibility(initOverrides) {
        const response = await this.getArchiveEligibilityRaw(initOverrides);
        return await response.value();
    }
    /**
     * Retrieves a bounded neighborhood of audit events around a single anchor log for operator investigation. Tenant visibility rules are enforced exactly like the standard audit search.
     * Get audit log context around an anchor event
     */
    async getAuditLogContextRaw(requestParameters, initOverrides) {
        if (requestParameters['auditLogId'] == null) {
            throw new runtime.RequiredError('auditLogId', 'Required parameter "auditLogId" was null or undefined when calling getAuditLogContext().');
        }
        const queryParameters = {};
        if (requestParameters['beforeCount'] != null) {
            queryParameters['beforeCount'] = requestParameters['beforeCount'];
        }
        if (requestParameters['afterCount'] != null) {
            queryParameters['afterCount'] = requestParameters['afterCount'];
        }
        if (requestParameters['tenantId'] != null) {
            queryParameters['tenantId'] = requestParameters['tenantId'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/{auditLogId}/context`.replace(`{${"auditLogId"}}`, encodeURIComponent(String(requestParameters['auditLogId']))),
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Retrieves a bounded neighborhood of audit events around a single anchor log for operator investigation. Tenant visibility rules are enforced exactly like the standard audit search.
     * Get audit log context around an anchor event
     */
    async getAuditLogContext(requestParameters, initOverrides) {
        const response = await this.getAuditLogContextRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Retrieves audit logs with optional filters, tenant-scoped visibility, and pagination for security monitoring and compliance reporting. Tenant Admins automatically see only their tenant\'s audit logs. Global Admins see all logs and may optionally filter by tenantId. Supports dynamic sorting via ?sort=field,direction (e.g., ?sort=auditLogId,asc). Default sort is by creation date descending (newest first).
     * Query audit logs
     */
    async getAuditLogsRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['eventType'] != null) {
            queryParameters['eventType'] = requestParameters['eventType'];
        }
        if (requestParameters['eventTypeFamily'] != null) {
            queryParameters['eventTypeFamily'] = requestParameters['eventTypeFamily'];
        }
        if (requestParameters['eventStatus'] != null) {
            queryParameters['eventStatus'] = requestParameters['eventStatus'];
        }
        if (requestParameters['apiName'] != null) {
            queryParameters['apiName'] = requestParameters['apiName'];
        }
        if (requestParameters['enrollmentId'] != null) {
            queryParameters['enrollmentId'] = requestParameters['enrollmentId'];
        }
        if (requestParameters['authAttemptId'] != null) {
            queryParameters['authAttemptId'] = requestParameters['authAttemptId'];
        }
        if (requestParameters['integrationId'] != null) {
            queryParameters['integrationId'] = requestParameters['integrationId'];
        }
        if (requestParameters['adminId'] != null) {
            queryParameters['adminId'] = requestParameters['adminId'];
        }
        if (requestParameters['targetAdminId'] != null) {
            queryParameters['targetAdminId'] = requestParameters['targetAdminId'];
        }
        if (requestParameters['tenantId'] != null) {
            queryParameters['tenantId'] = requestParameters['tenantId'];
        }
        if (requestParameters['createdAfter'] != null) {
            queryParameters['createdAfter'] = requestParameters['createdAfter'];
        }
        if (requestParameters['createdBefore'] != null) {
            queryParameters['createdBefore'] = requestParameters['createdBefore'];
        }
        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }
        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }
        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Retrieves audit logs with optional filters, tenant-scoped visibility, and pagination for security monitoring and compliance reporting. Tenant Admins automatically see only their tenant\'s audit logs. Global Admins see all logs and may optionally filter by tenantId. Supports dynamic sorting via ?sort=field,direction (e.g., ?sort=auditLogId,asc). Default sort is by creation date descending (newest first).
     * Query audit logs
     */
    async getAuditLogs(requestParameters = {}, initOverrides) {
        const response = await this.getAuditLogsRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Paginated search over audit chain checkpoints with optional filters (window range, entry count, checkpoint type, created range). Supports SEAL range selection and Declare Gap anchor lookup. Global Admin only. Default sort: windowStart,asc.
     * Search audit chain checkpoints
     */
    async getChainCheckpointsRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['windowStartAfter'] != null) {
            queryParameters['windowStartAfter'] = requestParameters['windowStartAfter'];
        }
        if (requestParameters['windowStartBefore'] != null) {
            queryParameters['windowStartBefore'] = requestParameters['windowStartBefore'];
        }
        if (requestParameters['entryCountMin'] != null) {
            queryParameters['entryCountMin'] = requestParameters['entryCountMin'];
        }
        if (requestParameters['entryCountMax'] != null) {
            queryParameters['entryCountMax'] = requestParameters['entryCountMax'];
        }
        if (requestParameters['checkpointType'] != null) {
            queryParameters['checkpointType'] = requestParameters['checkpointType'];
        }
        if (requestParameters['createdAfter'] != null) {
            queryParameters['createdAfter'] = requestParameters['createdAfter'];
        }
        if (requestParameters['createdBefore'] != null) {
            queryParameters['createdBefore'] = requestParameters['createdBefore'];
        }
        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }
        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }
        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/chain-checkpoints`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Paginated search over audit chain checkpoints with optional filters (window range, entry count, checkpoint type, created range). Supports SEAL range selection and Declare Gap anchor lookup. Global Admin only. Default sort: windowStart,asc.
     * Search audit chain checkpoints
     */
    async getChainCheckpoints(requestParameters = {}, initOverrides) {
        const response = await this.getChainCheckpointsRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Paginated list of operational incidents raised when peripheral supervision detected stalled checkpoints (bounded unsupervised activity / degraded mode). Separate from GAP_DECLARATION checkpoints. Global Admin only.
     * List audit-chain heartbeat incidents
     */
    async listLifecycleIncidentsRaw(requestParameters, initOverrides) {
        const queryParameters = {};
        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }
        if (requestParameters['size'] != null) {
            queryParameters['size'] = requestParameters['size'];
        }
        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }
        const headerParameters = {};
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/incidents`,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Paginated list of operational incidents raised when peripheral supervision detected stalled checkpoints (bounded unsupervised activity / degraded mode). Separate from GAP_DECLARATION checkpoints. Global Admin only.
     * List audit-chain heartbeat incidents
     */
    async listLifecycleIncidents(requestParameters = {}, initOverrides) {
        const response = await this.listLifecycleIncidentsRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Creates a MANIPULATION_CONCILIATION checkpoint spanning failBoundary to resumeBoundary, removes REGULAR checkpoints in that window, re-chains downstream checkpoints, emits an audit meta-event, and resolves the open AUDIT_INTEGRITY_RUPTURE alert. Blocked while AUDIT_CHAIN_HEARTBEAT_STALE is OPEN. Global Admin only.  Identify the alert by `alertId` or `dedupeKey` (exactly one). Boundaries must match the alert payload failBoundary/resumeBoundary (or windowStart/windowEnd when those are absent).
     * Reconcile an integrity rupture alert
     */
    async reconcileIntegrityRuptureRaw(requestParameters, initOverrides) {
        if (requestParameters['integrityRuptureReconciliationRequest'] == null) {
            throw new runtime.RequiredError('integrityRuptureReconciliationRequest', 'Required parameter "integrityRuptureReconciliationRequest" was null or undefined when calling reconcileIntegrityRupture().');
        }
        const queryParameters = {};
        const headerParameters = {};
        headerParameters['Content-Type'] = 'application/json';
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/reconcile-integrity-rupture`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['integrityRuptureReconciliationRequest'],
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Creates a MANIPULATION_CONCILIATION checkpoint spanning failBoundary to resumeBoundary, removes REGULAR checkpoints in that window, re-chains downstream checkpoints, emits an audit meta-event, and resolves the open AUDIT_INTEGRITY_RUPTURE alert. Blocked while AUDIT_CHAIN_HEARTBEAT_STALE is OPEN. Global Admin only.  Identify the alert by `alertId` or `dedupeKey` (exactly one). Boundaries must match the alert payload failBoundary/resumeBoundary (or windowStart/windowEnd when those are absent).
     * Reconcile an integrity rupture alert
     */
    async reconcileIntegrityRupture(requestParameters, initOverrides) {
        const response = await this.reconcileIntegrityRuptureRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Detective-layer validation over a date range: same semantics as the nightly batch. May raise or touch AUDIT_INTEGRITY_RUPTURE when violations remain alert-eligible. Global Admin only. Use GET integrity-check / chain-integrity for read-only forensic verify.
     * Run retroactive integrity validation
     */
    async runRetroactiveIntegrityValidationRaw(requestParameters, initOverrides) {
        if (requestParameters['retroactiveIntegrityValidationRunRequest'] == null) {
            throw new runtime.RequiredError('retroactiveIntegrityValidationRunRequest', 'Required parameter "retroactiveIntegrityValidationRunRequest" was null or undefined when calling runRetroactiveIntegrityValidation().');
        }
        const queryParameters = {};
        const headerParameters = {};
        headerParameters['Content-Type'] = 'application/json';
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/integrity-validation/run`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['retroactiveIntegrityValidationRunRequest'],
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Detective-layer validation over a date range: same semantics as the nightly batch. May raise or touch AUDIT_INTEGRITY_RUPTURE when violations remain alert-eligible. Global Admin only. Use GET integrity-check / chain-integrity for read-only forensic verify.
     * Run retroactive integrity validation
     */
    async runRetroactiveIntegrityValidation(requestParameters, initOverrides) {
        const response = await this.runRetroactiveIntegrityValidationRaw(requestParameters, initOverrides);
        return await response.value();
    }
    /**
     * Marks all chain checkpoints in the specified period as ARCHIVE_SEAL prior to dropping the corresponding DB partition. Runs a mandatory pre-flight integrity check -- rejected if any violation is detected. Returns the seal HMAC to include in the Git archive manifest. Global Admin only.  **Period identification -- two alternative modes:** - **Timestamp mode**: provide `periodStart` (inclusive) and `periodEnd` (exclusive) as ISO-8601 timestamps. - **Checkpoint ID mode**: provide `checkpointIdFrom` and `checkpointIdTo` (both inclusive integers). The service resolves the effective time range from those checkpoints automatically. Ergonomic when working directly with the database -- short IDs are easier to read than full timestamps.  Exactly one mode must be used. Providing both is an error.
     * Seal an audit chain period for archival
     */
    async sealArchiveRaw(requestParameters, initOverrides) {
        if (requestParameters['archiveSealRequest'] == null) {
            throw new runtime.RequiredError('archiveSealRequest', 'Required parameter "archiveSealRequest" was null or undefined when calling sealArchive().');
        }
        const queryParameters = {};
        const headerParameters = {};
        headerParameters['Content-Type'] = 'application/json';
        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);
            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const response = await this.request({
            path: `/api/v1/audit-logs/lifecycle/seal-archive`,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['archiveSealRequest'],
        }, initOverrides);
        return new runtime.JSONApiResponse(response);
    }
    /**
     * Marks all chain checkpoints in the specified period as ARCHIVE_SEAL prior to dropping the corresponding DB partition. Runs a mandatory pre-flight integrity check -- rejected if any violation is detected. Returns the seal HMAC to include in the Git archive manifest. Global Admin only.  **Period identification -- two alternative modes:** - **Timestamp mode**: provide `periodStart` (inclusive) and `periodEnd` (exclusive) as ISO-8601 timestamps. - **Checkpoint ID mode**: provide `checkpointIdFrom` and `checkpointIdTo` (both inclusive integers). The service resolves the effective time range from those checkpoints automatically. Ergonomic when working directly with the database -- short IDs are easier to read than full timestamps.  Exactly one mode must be used. Providing both is an error.
     * Seal an audit chain period for archival
     */
    async sealArchive(requestParameters, initOverrides) {
        const response = await this.sealArchiveRaw(requestParameters, initOverrides);
        return await response.value();
    }
}
exports.AuditLogsApi = AuditLogsApi;
/**
 * @export
 */
exports.GetAuditLogsEventTypeEnum = {
    AdminLogin: 'ADMIN_LOGIN',
    AdminLogout: 'ADMIN_LOGOUT',
    AdminPasswordChange: 'ADMIN_PASSWORD_CHANGE',
    AdminRecoveryUse: 'ADMIN_RECOVERY_USE',
    AdminRecoveryCodesIssued: 'ADMIN_RECOVERY_CODES_ISSUED',
    AdminRecoveryCodesRegenerated: 'ADMIN_RECOVERY_CODES_REGENERATED',
    AdminActivation: 'ADMIN_ACTIVATION',
    AdminActivationCodeReissued: 'ADMIN_ACTIVATION_CODE_REISSUED',
    AdminRecoveryEnrollmentReset: 'ADMIN_RECOVERY_ENROLLMENT_RESET',
    AdminCreated: 'ADMIN_CREATED',
    AdminProfileUpdated: 'ADMIN_PROFILE_UPDATED',
    AdminDeactivated: 'ADMIN_DEACTIVATED',
    AdminActivated: 'ADMIN_ACTIVATED',
    EnrollmentCreated: 'ENROLLMENT_CREATED',
    EnrollmentUpdated: 'ENROLLMENT_UPDATED',
    EnrollmentDeleted: 'ENROLLMENT_DELETED',
    EnrollmentBind: 'ENROLLMENT_BIND',
    EnrollmentVerify: 'ENROLLMENT_VERIFY',
    EnrollmentRevoked: 'ENROLLMENT_REVOKED',
    EnrollmentDeactivated: 'ENROLLMENT_DEACTIVATED',
    EnrollmentReactivated: 'ENROLLMENT_REACTIVATED',
    EnrollmentAuthAttemptBlocked: 'ENROLLMENT_AUTH_ATTEMPT_BLOCKED',
    EnrollmentExpired: 'ENROLLMENT_EXPIRED',
    AuthAttemptCreated: 'AUTH_ATTEMPT_CREATED',
    AuthAttemptPending: 'AUTH_ATTEMPT_PENDING',
    AuthAttemptRespond: 'AUTH_ATTEMPT_RESPOND',
    AuthAttemptCancelled: 'AUTH_ATTEMPT_CANCELLED',
    AuthAttemptExpired: 'AUTH_ATTEMPT_EXPIRED',
    ApiKeyCreated: 'API_KEY_CREATED',
    ApiKeyUpdated: 'API_KEY_UPDATED',
    ApiKeyRevoked: 'API_KEY_REVOKED',
    ApiKeyExpired: 'API_KEY_EXPIRED',
    ApiKeyAuthSuccess: 'API_KEY_AUTH_SUCCESS',
    ApiKeyAuthFailed: 'API_KEY_AUTH_FAILED',
    ApiKeyIpBlocked: 'API_KEY_IP_BLOCKED',
    SystemError: 'SYSTEM_ERROR',
    KeyIntroduced: 'KEY_INTRODUCED',
    KeyPromotedPrimary: 'KEY_PROMOTED_PRIMARY',
    KeyDemoted: 'KEY_DEMOTED',
    KeyDisabled: 'KEY_DISABLED',
    KeysetBackupCreated: 'KEYSET_BACKUP_CREATED',
    ReencryptionStarted: 'REENCRYPTION_STARTED',
    ReencryptionBatchProgress: 'REENCRYPTION_BATCH_PROGRESS',
    ReencryptionCompleted: 'REENCRYPTION_COMPLETED',
    ReencryptionFailed: 'REENCRYPTION_FAILED',
    ReencryptionResumed: 'REENCRYPTION_RESUMED',
    ReencryptionPaused: 'REENCRYPTION_PAUSED',
    IntegrationCreated: 'INTEGRATION_CREATED',
    IntegrationUpdated: 'INTEGRATION_UPDATED',
    IntegrationRetired: 'INTEGRATION_RETIRED',
    IntegrationDeleted: 'INTEGRATION_DELETED',
    TenantCreated: 'TENANT_CREATED',
    TenantUpdated: 'TENANT_UPDATED',
    TenantDeactivated: 'TENANT_DEACTIVATED',
    TenantActivated: 'TENANT_ACTIVATED',
    EvaluatorSelfRegistration: 'EVALUATOR_SELF_REGISTRATION',
    AuditChainArchiveSealed: 'AUDIT_CHAIN_ARCHIVE_SEALED',
    AuditChainArchiveExported: 'AUDIT_CHAIN_ARCHIVE_EXPORTED',
    AuditChainGapDeclared: 'AUDIT_CHAIN_GAP_DECLARED',
    AuditChainIncidentDeclared: 'AUDIT_CHAIN_INCIDENT_DECLARED',
    AuditIntegrityRuptureConciliated: 'AUDIT_INTEGRITY_RUPTURE_CONCILIATED',
    AuditEntryIntegrityConciliated: 'AUDIT_ENTRY_INTEGRITY_CONCILIATED',
    NightlyIntegrityValidationCompleted: 'NIGHTLY_INTEGRITY_VALIDATION_COMPLETED',
    AlertRaised: 'ALERT_RAISED',
    AlertResolved: 'ALERT_RESOLVED'
};
/**
 * @export
 */
exports.GetAuditLogsEventTypeFamilyEnum = {
    Admin: 'ADMIN',
    Enrollment: 'ENROLLMENT',
    AuthAttempt: 'AUTH_ATTEMPT',
    ApiKey: 'API_KEY',
    System: 'SYSTEM',
    EncryptionKey: 'ENCRYPTION_KEY',
    Reencryption: 'REENCRYPTION',
    Integration: 'INTEGRATION',
    Tenant: 'TENANT',
    AuditChain: 'AUDIT_CHAIN',
    Alert: 'ALERT'
};
/**
 * @export
 */
exports.GetAuditLogsEventStatusEnum = {
    Success: 'SUCCESS',
    Failure: 'FAILURE',
    Error: 'ERROR'
};
/**
 * @export
 */
exports.GetAuditLogsApiNameEnum = {
    AdminApi: 'ADMIN_API',
    AuthApi: 'AUTH_API',
    IntegrationApi: 'INTEGRATION_API'
};
/**
 * @export
 */
exports.GetChainCheckpointsCheckpointTypeEnum = {
    Regular: 'REGULAR',
    ArchiveSeal: 'ARCHIVE_SEAL',
    GapDeclaration: 'GAP_DECLARATION',
    ManipulationConciliation: 'MANIPULATION_CONCILIATION'
};
//# sourceMappingURL=AuditLogsApi.js.map