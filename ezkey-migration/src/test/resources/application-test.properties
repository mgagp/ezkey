# Test configuration for ezkey-migration
# Set active profile to test to disable CommandLineRunner
spring.profiles.active=test
# Disable Spring Boot from starting the application during tests
spring.main.web-application-type=none
# Disable CommandLineRunner during tests
spring.main.lazy-initialization=true

