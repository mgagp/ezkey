---
# Deny-path variant — `respond_accepted: false`; still verifies integration-signed respond payload when API returns one.
scenario_label: protocol-lab-cleanstart-reject

base_urls:
  admin: "http://localhost:9080"
  auth: "http://localhost:8080"
  crypto: "http://localhost:9090"

integration_id: 1

admin_bearer_token: ""

enrollment:
  name: "Bob Okonkwo"
  auth_attempt_challenge_required: false

scenario:
  auth_challenge_requested: false
  respond_accepted: false

wait:
  timeout_seconds: 120
  poll_seconds: 2

device_private_key_storage_tier: "STANDARD"
