# Test/QA build: same as production (relative URLs) but demo mode enabled for Docker demos.
# Used by: npm run build:test (e.g. default Docker build via start.sh).
VITE_API_BASE_URL=
VITE_DEMO_MODE=true
