# Empty = relative URLs (/api/...) — the reverse proxy (Caddy, nginx, etc.)
# handles forwarding /api/* to the Admin API.
# Set to an absolute URL only if the Admin API is on a different origin.
VITE_API_BASE_URL=
