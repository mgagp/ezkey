<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- statusBarColor and navigationBarColor removed: Android 15 (API 35) forces edge-to-edge
             and ignores these attributes. Bar appearance is handled via StatusBar (translucent)
             and useSafeAreaInsets in the React Native layer. -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <!--
      Android 12+ native splash screen, backported via androidx.core:core-splashscreen.
      Renders the launcher icon foreground centered on the app shell background, then transitions
      to AppTheme via installSplashScreen() in MainActivity.
    -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

</resources>
