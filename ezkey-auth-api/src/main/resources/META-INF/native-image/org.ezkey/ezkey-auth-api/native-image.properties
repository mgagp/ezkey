# GraalVM Native Image Build Arguments
# 
# NOTE: This file contains GraalVM build options that CANNOT be configured via
# Spring AOT RuntimeHints API. The Spring AOT API only supports:
# - Reflection hints
# - Resource patterns
# - Serialization hints
# - Proxy registration
#
# GraalVM build options like --initialize-at-run-time must be configured here.
# This is a limitation of the Spring AOT API, not a design choice.
#
# For details, see: AuthNativeConfiguration.java (Java-based configuration)

Args = --no-fallback \
       -H:+ReportExceptionStackTraces \
       -H:+AddAllCharsets \
       --enable-all-security-services \
       -H:IncludeResourceBundles=org.hibernate.validator.ValidationMessages \
       -H:+StaticExecutableWithDynamicLibC \
       -H:+RemoveSaturatedTypeFlows \
       --allow-incomplete-classpath \
       --report-unsupported-elements-at-runtime \
       -H:+AllowDeprecatedBuilderClassesOnImageClasspath \
       -H:+UnlockExperimentalVMOptions \
       -H:FallbackThreshold=0 \
       -H:+PrintAnalysisCallTree \
       --initialize-at-build-time=org.hibernate.internal.HEMLogging,org.hibernate.internal.CoreLogging,org.hibernate.jpa.boot.internal.EntityManagerFactoryBuilderImpl \
       --initialize-at-run-time=org.jboss.logging,org.hibernate.sql.ast.tree \
       --include-classes=org.hibernate.sql.ast.tree.**
