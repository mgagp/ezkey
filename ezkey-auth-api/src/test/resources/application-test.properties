# Test configuration for ezkey-auth-api
# This file disables rate limiting for unit tests

# Disable rate limiting in tests
ezkey.rate-limit.enabled=false

# Unit tests — avoid coupling auth flows to checkpoint heartbeat supervision.
ezkey.audit.chain.heartbeat.enabled=false
