# ACME Demo Application Configuration
# Demonstrates EZKey login integration with frontend-first approach
server.port=8082

# Application metadata
spring.application.name=ezkey-demo-app-acme
info.app.name=ACME Inc Demo Application
info.app.description=Demo application showcasing EZKey passwordless login
info.app.version=2.0.0

# Thymeleaf configuration
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Static resources configuration
spring.web.resources.static-locations=classpath:/static/
spring.web.resources.cache.period=3600

# =============================================================================
# EZKey Integration Configuration
# =============================================================================
# Admin API URL - used by frontend JavaScript for login calls
# For Docker: http://localhost:9080 (browser accesses host-mapped port)
# For IDE: http://localhost:9080
ezkey.admin.api.url=http://localhost:9080

# Login mode: frontend (default) or backend (future)
# - frontend: JavaScript calls Admin API directly from browser
# - backend: Server-side calls via RestTemplate (not implemented yet)
ezkey.login.mode=frontend

# =============================================================================
# Logging configuration
# =============================================================================
logging.level.org.ezkey.demo.acme=DEBUG
logging.level.org.springframework.web=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Development configuration
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

spring.jackson.deserialization.ADJUST_DATES_TO_CONTEXT_TIME_ZONE=false
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSSSSS
