<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.ezkey</groupId>
	<artifactId>ezkey-parent</artifactId>
	<version>${revision}${buildQualifier}${changelist}</version>
	<packaging>pom</packaging>

	<name>Ezkey Parent</name>
	<description>Ezkey - Open Source MFA/Passkey Alternative</description>
	<url>https://github.com/ezkey/ezkey</url>

	<licenses>
		<license>
			<name>MIT License</name>
			<url>https://opensource.org/licenses/MIT</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<developers>
		<developer>
			<name>Ezkey Contributors</name>
			<email>info@ezkey.org</email>
			<organization>Ezkey Project</organization>
			<organizationUrl>https://ezkey.org</organizationUrl>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:git://github.com/ezkey/ezkey.git</connection>
		<developerConnection>scm:git:ssh://github.com:ezkey/ezkey.git</developerConnection>
		<tag>HEAD</tag>
		<url>https://github.com/ezkey/ezkey</url>
	</scm>

	<!-- Modules du projet -->
	<modules>
		<module>checkstyle-config</module>
		<module>ezkey-core</module>
		<module>ezkey-core-security</module>
		<module>ezkey-migration</module>
		<module>ezkey-admin-api</module>
		<module>ezkey-auth-api</module>
		<module>ezkey-integration-api</module>
		<module>ezkey-crypto-api</module>
		<module>ezkey-sdk/java</module>
		<module>ezkey-demo-app-acme</module>
		<module>ezkey-demo-device</module>
		<module>ezkey-tests</module>
	</modules>

	<properties>
		<!-- CI-friendly version properties -->
		<revision>0.0.1</revision>
		<changelist>-SNAPSHOT</changelist>
		<!-- buildQualifier is injected by wrapper scripts or .mvn/maven.config (empty by default) -->
		<buildQualifier></buildQualifier>
		<java.version>25</java.version>
		<maven.compiler.source>25</maven.compiler.source>
		<maven.compiler.target>25</maven.compiler.target>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<spring-boot.version>4.1.1</spring-boot.version>
		<!-- SEC-019: targeted BOM overrides (July 2026 Security Challenge advisories) -->
		<postgresql.version>42.7.13</postgresql.version>
		<logback.version>1.6.3</logback.version>
		<tomcat.version>11.0.25</tomcat.version>

		<jackson-bom.version>3.2.2</jackson-bom.version>
		<jackson-2-bom.version>2.22.1</jackson-2-bom.version>
		<mapstruct.version>1.6.3</mapstruct.version>
		<springdoc.version>3.1.0</springdoc.version>
		<!-- SpotBugs engine + Maven plugin (report-only via java-doctor-curated; not a build gate) -->
		<spotbugs.version>4.10.4</spotbugs.version>
		<spotbugs-maven-plugin.version>4.10.4.0</spotbugs-maven-plugin.version>
		<checkstyle.version>3.6.0</checkstyle.version>
		<!-- maven-pmd-plugin version (narrow ruleset under config/java-doctor/; not a build gate) -->
		<pmd.version>3.28.0</pmd.version>
		<jacoco.version>0.8.15</jacoco.version>
		<zxing.version>3.5.4</zxing.version>
		<spotless.version>3.10.1</spotless.version>
		<!-- Nested Spotless pin; Dependabot typically misses the plugin XML. Pulse in dependabot-curated. -->
		<google-java-format.version>1.36.1</google-java-format.version>
		<bucket4j.version>8.19.0</bucket4j.version>
		<caffeine.version>3.2.4</caffeine.version>
		<tink.version>1.23.0</tink.version>
		<shedlock.version>7.7.0</shedlock.version>
		<javamelody.version>2.8.0</javamelody.version>
		<ipaddress.version>5.6.2</ipaddress.version>
		<license-maven-plugin.version>2.7.1</license-maven-plugin.version>
	</properties>

	<!-- Gestion centralisée des dépendances -->
	<dependencyManagement>
		<dependencies>
			<!-- Spring Boot BOM -->
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring-boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>

			<!-- SEC-019: explicit overrides (imported BOM keeps its own property values) -->
			<dependency>
				<groupId>org.postgresql</groupId>
				<artifactId>postgresql</artifactId>
				<version>${postgresql.version}</version>
			</dependency>
			<dependency>
				<groupId>ch.qos.logback</groupId>
				<artifactId>logback-classic</artifactId>
				<version>${logback.version}</version>
			</dependency>
			<dependency>
				<groupId>ch.qos.logback</groupId>
				<artifactId>logback-core</artifactId>
				<version>${logback.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-core</artifactId>
				<version>${tomcat.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-el</artifactId>
				<version>${tomcat.version}</version>
			</dependency>
			<dependency>
				<groupId>org.apache.tomcat.embed</groupId>
				<artifactId>tomcat-embed-websocket</artifactId>
				<version>${tomcat.version}</version>
			</dependency>

			<!-- MapStruct -->
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct</artifactId>
				<version>${mapstruct.version}</version>
			</dependency>
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct-processor</artifactId>
				<version>${mapstruct.version}</version>
			</dependency>

			<!-- SpringDoc OpenAPI -->
			<dependency>
				<groupId>org.springdoc</groupId>
				<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
				<version>${springdoc.version}</version>
			</dependency>

			<!-- ZXing QR Code -->
			<dependency>
				<groupId>com.google.zxing</groupId>
				<artifactId>core</artifactId>
				<version>${zxing.version}</version>
			</dependency>
			<dependency>
				<groupId>com.google.zxing</groupId>
				<artifactId>javase</artifactId>
				<version>${zxing.version}</version>
			</dependency>

			<!-- Rate Limiting -->
			<dependency>
				<groupId>com.bucket4j</groupId>
				<artifactId>bucket4j_jdk17-core</artifactId>
				<version>${bucket4j.version}</version>
			</dependency>
			<dependency>
				<groupId>com.github.ben-manes.caffeine</groupId>
				<artifactId>caffeine</artifactId>
				<version>${caffeine.version}</version>
			</dependency>

			<!-- Tink / ShedLock / IPAddress (same parent-pin shape as ZXing and Bucket4j) -->
			<dependency>
				<groupId>com.google.crypto.tink</groupId>
				<artifactId>tink</artifactId>
				<version>${tink.version}</version>
			</dependency>
			<dependency>
				<groupId>net.javacrumbs.shedlock</groupId>
				<artifactId>shedlock-spring</artifactId>
				<version>${shedlock.version}</version>
			</dependency>
			<dependency>
				<groupId>net.javacrumbs.shedlock</groupId>
				<artifactId>shedlock-provider-jdbc-template</artifactId>
				<version>${shedlock.version}</version>
			</dependency>
			<dependency>
				<groupId>com.github.seancfoley</groupId>
				<artifactId>ipaddress</artifactId>
				<version>${ipaddress.version}</version>
			</dependency>
			<dependency>
				<groupId>net.bull.javamelody</groupId>
				<artifactId>javamelody-spring-boot4-starter</artifactId>
				<version>${javamelody.version}</version>
			</dependency>

			<!-- Modules internes -->
			<dependency>
				<groupId>org.ezkey</groupId>
				<artifactId>ezkey-core</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>org.ezkey</groupId>
				<artifactId>ezkey-core-security</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>org.ezkey</groupId>
				<artifactId>ezkey-sdk</artifactId>
				<version>${project.version}</version>
			</dependency>

			<!-- SEC-019: Jackson BOMs after spring-boot-dependencies (later import wins) -->
			<dependency>
				<groupId>tools.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>${jackson-bom.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>${jackson-2-bom.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- Force engine + annotations alignment: Jackson 3.2 needs annotations 2.22+
			     (JsonApplyView). Springdoc/swagger still pulls annotations 2.21 transitively;
			     Boot may leave tools.jackson-core behind the forced databind pin. -->
			<dependency>
				<groupId>tools.jackson.core</groupId>
				<artifactId>jackson-databind</artifactId>
				<version>${jackson-bom.version}</version>
			</dependency>
			<dependency>
				<groupId>tools.jackson.core</groupId>
				<artifactId>jackson-core</artifactId>
				<version>${jackson-bom.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-databind</artifactId>
				<version>${jackson-2-bom.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fasterxml.jackson.core</groupId>
				<artifactId>jackson-annotations</artifactId>
				<!-- Annotations drop the patch digit (jackson-2-bom property jackson.version.annotations). -->
				<version>2.22</version>
			</dependency>


		</dependencies>
	</dependencyManagement>

	<!-- Configuration des plugins partagés -->
	<build>
		<pluginManagement>
			<plugins>
				<!-- Spotless Plugin (Google Java Format) -->
				<plugin>
					<groupId>com.diffplug.spotless</groupId>
					<artifactId>spotless-maven-plugin</artifactId>
					<version>${spotless.version}</version>
					<configuration>
						<java>
							<includes>
								<include>src/main/java/**/*.java</include>
								<include>src/test/java/**/*.java</include>
							</includes>
							<!-- Enforce LF line endings per Google Java Style Guide -->
							<endWithNewline/>
							<removeUnusedImports/>
							<trimTrailingWhitespace/>
							<googleJavaFormat>
								<version>${google-java-format.version}</version>
								<style>GOOGLE</style>
								<reflowLongStrings>true</reflowLongStrings>
								<formatJavadoc>true</formatJavadoc>
							</googleJavaFormat>
						</java>
					</configuration>
					<executions>
						<execution>
							<goals>
								<goal>apply</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<!-- Checkstyle Plugin -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-checkstyle-plugin</artifactId>
					<version>${checkstyle.version}</version>

					<dependencies>
    					<dependency>
      						<groupId>com.puppycrawl.tools</groupId>
      						<artifactId>checkstyle</artifactId>
      						<version>14.0.0</version> <!-- Override default -->
    					</dependency>
						<dependency>
							<groupId>org.ezkey</groupId>
							<artifactId>checkstyle-config</artifactId>
							<version>${project.version}</version>
						</dependency>
					</dependencies>

					<configuration>
						<configLocation>checkstyle.xml</configLocation>
						<suppressionsLocation>checkstyle-suppressions.xml</suppressionsLocation>
						<suppressionsFileExpression>checkstyle.suppressions.file</suppressionsFileExpression>
						<consoleOutput>true</consoleOutput>
						<failsOnError>true</failsOnError>
						<includeTestSourceDirectory>false</includeTestSourceDirectory>
						<violationSeverity>warning</violationSeverity>
					</configuration>
					<executions>
						<execution>
							<id>validate</id>
							<phase>validate</phase>
							<goals>
								<goal>check</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<!-- Maven Compiler Plugin -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.15.0</version>
					<configuration>
						<release>${java.version}</release>
						<parameters>true</parameters>
						<annotationProcessorPaths>
							<path>
								<groupId>org.mapstruct</groupId>
								<artifactId>mapstruct-processor</artifactId>
								<version>${mapstruct.version}</version>
							</path>
						</annotationProcessorPaths>
					</configuration>
				</plugin>

				<!-- Maven Surefire Plugin -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>3.5.6</version>
					<configuration>
						<includes>
							<include>**/*Test.java</include>
							<include>**/*Tests.java</include>
						</includes>
						<excludes>
							<exclude>**/*IntegrationTest.java</exclude>
						</excludes>
					</configuration>
				</plugin>

				<!-- Maven Failsafe Plugin -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-failsafe-plugin</artifactId>
					<version>3.5.6</version>
					<configuration>
						<includes>
							<include>**/*IntegrationTest.java</include>
						</includes>
					</configuration>
					<executions>
						<execution>
							<goals>
								<goal>integration-test</goal>
								<goal>verify</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<!-- JaCoCo Code Coverage -->
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco.version}</version>
					<executions>
						<execution>
							<goals>
								<goal>prepare-agent</goal>
							</goals>
						</execution>
						<execution>
							<id>report</id>
							<phase>test</phase>
							<goals>
								<goal>report</goal>
							</goals>
						</execution>
					</executions>
				</plugin>

				<!-- Spring Boot Maven Plugin -->
				<plugin>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-maven-plugin</artifactId>
					<version>${spring-boot.version}</version>
					<configuration>
						<excludes>
							<exclude>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
							</exclude>
						</excludes>
						<includes>
							<include>
								<groupId>nothing</groupId>
								<artifactId>nothing</artifactId>
							</include>
						</includes>
					</configuration>
					<executions>
						<execution>
							<goals>
								<goal>repackage</goal>
							</goals>
							<configuration>
								<layers>
									<enabled>false</enabled>
								</layers>
							</configuration>
						</execution>
					</executions>
				</plugin>

				<!-- Maven Resources Plugin - Include license files -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-resources-plugin</artifactId>
					<version>3.5.0</version>
					<configuration>
						<nonFilteredFileExtensions>
							<nonFilteredFileExtension>txt</nonFilteredFileExtension>
						</nonFilteredFileExtensions>
					</configuration>
				</plugin>

				<!-- SpotBugs (report-only; invoked by scripts/java-doctor-curated.sh) -->
				<plugin>
					<groupId>com.github.spotbugs</groupId>
					<artifactId>spotbugs-maven-plugin</artifactId>
					<version>${spotbugs-maven-plugin.version}</version>
					<configuration>
						<failOnError>false</failOnError>
						<xmlOutput>true</xmlOutput>
						<xmlOutputDirectory>${project.build.directory}</xmlOutputDirectory>
						<effort>Default</effort>
						<threshold>Low</threshold>
						<excludeFilterFile>${maven.multiModuleProjectDirectory}/config/java-doctor/spotbugs-exclude.xml</excludeFilterFile>
					</configuration>
					<dependencies>
						<dependency>
							<groupId>com.github.spotbugs</groupId>
							<artifactId>spotbugs</artifactId>
							<version>${spotbugs.version}</version>
						</dependency>
					</dependencies>
				</plugin>

				<!-- PMD (narrow design/maintainability ruleset; report-only) -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-pmd-plugin</artifactId>
					<version>${pmd.version}</version>
					<configuration>
						<failOnViolation>false</failOnViolation>
						<printFailingErrors>true</printFailingErrors>
						<linkXRef>false</linkXRef>
						<includeTests>false</includeTests>
						<rulesets>
							<ruleset>${maven.multiModuleProjectDirectory}/config/java-doctor/pmd-ruleset.xml</ruleset>
						</rulesets>
						<excludeRoots>
							<excludeRoot>${project.build.directory}/generated-sources</excludeRoot>
						</excludeRoots>
					</configuration>
				</plugin>

				<!-- License Maven Plugin -->
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>license-maven-plugin</artifactId>
					<version>${license-maven-plugin.version}</version>
					<configuration>
						<outputDirectory>${project.basedir}</outputDirectory>
						<thirdPartyFilename>THIRD-PARTY.txt</thirdPartyFilename>
						<useMissingFile>true</useMissingFile>
						<missingFile>${project.basedir}/src/license/THIRD-PARTY.properties</missingFile>
						<licenseMerges>
							<licenseMerge>The Apache Software License, Version 2.0|Apache License, Version 2.0|Apache 2.0|Apache-2.0</licenseMerge>
							<licenseMerge>MIT License|MIT|The MIT License</licenseMerge>
							<licenseMerge>Eclipse Public License - v 1.0|EPL-1.0|Eclipse Public License 1.0</licenseMerge>
							<licenseMerge>GNU General Public License, Version 2.0|GPL 2.0|GPL-2.0</licenseMerge>
							<licenseMerge>GNU Lesser General Public License, Version 2.1|LGPL 2.1|LGPL-2.1</licenseMerge>
						</licenseMerges>
						<excludedGroups>org.ezkey</excludedGroups>
					</configuration>
					<executions>
						<execution>
							<id>aggregate-add-third-party</id>
							<goals>
								<goal>aggregate-add-third-party</goal>
							</goals>
							<phase>validate</phase>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>

		<!-- Plugins appliqués à tous les modules -->
		<plugins>
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>license-maven-plugin</artifactId>
			</plugin>
			<!-- Available for explicit goals only (spotbugs:spotbugs / pmd:pmd); no lifecycle binding -->
			<plugin>
				<groupId>com.github.spotbugs</groupId>
				<artifactId>spotbugs-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-pmd-plugin</artifactId>
			</plugin>

			<plugin>
				<groupId>org.openrewrite.maven</groupId>
				<artifactId>rewrite-maven-plugin</artifactId>
				<version>6.46.1</version>
				<configuration>
					<activeStyles>
						<style>org.openrewrite.java.GoogleJavaFormat</style>
					</activeStyles>
					<exclusions>
						<exclusion>**/ezkey_mobile/**</exclusion>
						<exclusion>**/ezkey-cli-python/**</exclusion>
						<exclusion>**/ezkey-pam/**</exclusion>
						<exclusion>**/ezkey-sdk/**</exclusion>
						<exclusion>ezkey_mobile/**</exclusion>
						<exclusion>ezkey-cli-python/**</exclusion>
						<exclusion>ezkey-pam/**</exclusion>
						<exclusion>ezkey-sdk/**</exclusion>
					</exclusions>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.openrewrite.recipe</groupId>
						<artifactId>rewrite-spring</artifactId>
						<version>6.37.0</version>
					</dependency>
				</dependencies>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!--
			Profile used for Docker image builds and other environments
			where we explicitly want to skip all Checkstyle execution.
			This keeps local Maven builds unchanged while ensuring that
			container builds are fast and do not depend on quality tooling.
		-->
		<profile>
			<id>no-checkstyle</id>
			<properties>
				<!-- Standard flag recognized by maven-checkstyle-plugin -->
				<checkstyle.skip>true</checkstyle.skip>
			</properties>
			<build>
				<pluginManagement>
					<plugins>
						<!-- Disable Checkstyle plugin completely in Docker builds -->
						<plugin>
							<groupId>org.apache.maven.plugins</groupId>
							<artifactId>maven-checkstyle-plugin</artifactId>
							<configuration>
								<skip>true</skip>
							</configuration>
							<dependencies>
								<!-- Override: Remove checkstyle-config dependency in Docker builds -->
								<!-- Empty dependencies list prevents resolution of checkstyle-config -->
							</dependencies>
							<executions>
								<execution>
									<id>validate</id>
									<phase>none</phase>
								</execution>
							</executions>
						</plugin>
					</plugins>
				</pluginManagement>
			</build>
		</profile>
	</profiles>
</project>
