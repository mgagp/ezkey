meta {
  name: create - bad credentials
  type: http
  seq: 4
}

post {
  url: {{base_url_integration_api}}/api/v1/auth-attempts
  body: json
  auth: basic
}

auth:basic {
  username: ezkey_ikey_invalid
  password: ezkey_skey_invalid
}

body:json {
  {
      "enrollmentId": 1,
      "challengeRequested": false
  }
}

script:post-response {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
}

docs {
  Uses invalid API key credentials. Expects 401 Unauthorized.
}
